{
  "schema_version": "v1",
  "manifest_id": "evaluate-size-class-barrier-cli-contract",
  "bead": "bd-51q2g",
  "generated_utc": "2026-05-13T00:00:00Z",
  "source_commit": "54431289",
  "purpose": "Pin the harness binary subcommand that exposes frankenlibc_membrane::runtime_math::sos_barrier::evaluate_size_class_barrier as a standalone CLI. The lib API computes the size-class admissibility barrier certificate over (requested_size, mapped_class_size, class_membership_valid) and returns an i64 score where positive => certified-safe headroom and negative => certificate violation. The SDP quadratic form penalizes waste ratio + membership violations + range violations + underflow. Previously reachable only from Rust callers; the CLI bridge lets CI verify size-class invariants without compiling.",
  "subcommand_name": "evaluate-size-class-barrier",
  "binary_target": "harness",
  "required_flags": [
    "--requested-size",
    "--mapped-class-size",
    "--output"
  ],
  "optional_flags": [
    "--class-membership-valid"
  ],
  "underlying_lib_function": "frankenlibc_membrane::runtime_math::sos_barrier::evaluate_size_class_barrier",
  "underlying_lib_functions": [
    "frankenlibc_membrane::runtime_math::sos_barrier::evaluate_size_class_barrier"
  ],
  "jsonl_output_contract": {
    "record_count": 1,
    "record_kind_marker": "size_class_barrier",
    "required_fields": [
      "kind",
      "requested_size",
      "mapped_class_size",
      "class_membership_valid",
      "headroom",
      "safe"
    ]
  },
  "policy": {
    "must_emit_exactly_one_jsonl_record": true,
    "echoes_inputs_into_output_record": true,
    "safe_iff_headroom_at_or_above_zero": true,
    "deterministic_given_inputs": true,
    "exact_class_size_match_with_valid_membership_certified_safe": true,
    "invalid_class_membership_violates_certificate": true,
    "underflow_violation_when_mapped_smaller_than_requested": true
  },
  "rejected_evidence_kinds": [
    "missing_required_flag",
    "wrong_record_kind",
    "safe_true_with_negative_headroom",
    "safe_false_with_non_negative_headroom",
    "non_deterministic_output",
    "underflow_certified_safe",
    "invalid_membership_certified_safe"
  ],
  "summary": {
    "bead": "bd-51q2g",
    "subcommand": "evaluate-size-class-barrier",
    "report_only": true,
    "claim_status": "report_only"
  }
}
