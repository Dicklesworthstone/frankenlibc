{
  "schema_version": "v1",
  "manifest_id": "evaluate-provenance-barrier-cli-contract",
  "bead": "bd-u5khi",
  "generated_utc": "2026-05-13T00:00:00Z",
  "source_commit": "047ca366f2315c3d40338a3a8674c7f96de7ab4a",
  "purpose": "Pin the harness binary subcommand that exposes frankenlibc_membrane::runtime_math::sos_barrier::evaluate_provenance_barrier as a standalone CLI. The lib API evaluates Invariant B (Pointer Provenance Admissibility) over four ppm inputs (risk_ppm, validation_depth_ppm 0=Fast 1M=Full, bloom_fp_rate_ppm, arena_pressure_ppm) and returns an i64 barrier value where negative => violation. Previously reachable only from Rust callers.",
  "subcommand_name": "evaluate-provenance-barrier",
  "binary_target": "harness",
  "required_flags": [
    "--risk-ppm",
    "--validation-depth-ppm",
    "--bloom-fp-rate-ppm",
    "--arena-pressure-ppm",
    "--output"
  ],
  "optional_flags": [],
  "underlying_lib_function": "frankenlibc_membrane::runtime_math::sos_barrier::evaluate_provenance_barrier",
  "underlying_lib_functions": [
    "frankenlibc_membrane::runtime_math::sos_barrier::evaluate_provenance_barrier"
  ],
  "jsonl_output_contract": {
    "record_count": 1,
    "record_kind_marker": "provenance_barrier",
    "required_fields": [
      "kind",
      "risk_ppm",
      "validation_depth_ppm",
      "bloom_fp_rate_ppm",
      "arena_pressure_ppm",
      "headroom",
      "safe"
    ]
  },
  "policy": {
    "must_emit_exactly_one_jsonl_record": true,
    "echoes_inputs_into_output_record": true,
    "safe_iff_headroom_at_or_above_zero": true,
    "deterministic_given_inputs": true,
    "low_risk_full_validation_certified_safe": true,
    "high_risk_fast_path_with_bad_bloom_violates_certificate": true
  },
  "rejected_evidence_kinds": [
    "missing_required_flag",
    "wrong_record_kind",
    "safe_true_with_negative_headroom",
    "safe_false_with_non_negative_headroom",
    "non_deterministic_output",
    "max_stress_certified_safe"
  ],
  "summary": {
    "bead": "bd-u5khi",
    "subcommand": "evaluate-provenance-barrier",
    "report_only": true,
    "claim_status": "report_only"
  }
}
