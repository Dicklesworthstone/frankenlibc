/target/
target-*/
legacy_glibc_code/
Cargo.lock
*.swp
*.swo
*~
.DS_Store
._*

# Stray image files at repo root (downloads dropped in wrong dir)
/*.jpeg
/*.jpg
/*.png

# Root-level agent scratch (ad-hoc reproducers compiled + source alongside)
/test_*
/test_*.c
/test_*.rs

# bv (beads viewer) local config and caches
.bv/

# Profiling and benchmark data
perf.data
perf.data.*
*.profraw
flamegraph*.svg
scripts/perf_baseline.json

# Local scratch DBs
storage.sqlite3
storage.sqlite3-*

# Build artifacts
a.out

# Beads history snapshots
.beads/.br_history/

# bv graph output
--graph-format.svg

# Python cache
__pycache__/
*.pyc

# Temporary directories
.tmp/

# Symlinked target directories
target_sym*/

# FastEmbed model caches (downloaded at runtime)
.fastembed_cache/

# Agent coordination config
.agent-mail.yaml

# Compiler crash reports
rustc-ice-*.txt

# Ephemeral agent flag/lock files
Safe

# Ephemeral files (agent-generated, build artifacts, test detritus)
ci-artifacts/
proptest-regressions/
test_meminfo

# Agent session ephemeral files
SESSION_TODO.md
SESSION_REPORT.md
SESSION_*.md
verify_*.sh
newfile.txt
repro_*.rs

# Nested crate build artifacts
crates/*/target/

# Codex/agent build artifacts
target-codex*/
target_*/

# Claude local settings + agent scratch dir (scheduled_tasks.lock, settings.local.json, etc.)
.claude/

# Agent mail project identity
.agent-mail-project-id

# SQLite backup/corruption/recovery files
*.sqlite3.bak
*.sqlite3-wal.bak
*.sqlite3.corrupt-*
*.sqlite3.corrupt_*
*.sqlite3.pre_recovery_*
storage.recover*.sql
storage.recovered.sqlite3

# Beads backup/recovery files (agent-generated snapshots)
.beads/*.bak.*
.beads/*.corrupt_*
.beads/*.pre_recovery_*
.beads/*.recover*.sql
.beads/*.orphan_*
.beads/.br_recovery/
.beads/beads.db.bad_*
.beads/beads.db-shm.bad_*
.beads/beads.db-wal.bad_*
.beads/beads.db.rebuild_*
.beads/beads.db.stale_*
.beads/recovery_*/
.beads_snapshot_*/
.beads.snapshot_*/
.beads.recovery_*/

# Ephemeral scratch files
baseline_decisions

# System trace/debug logs
strace.log
strace*.log
ltrace*.log

# Remote compilation helper artifacts
.rch-target/
.rch-deploy-root/
rch-bin/
rch-install/
.env

# Core dumps and agent config backups
core.*
core
*.mcp.json.*.bak
cline.mcp.json
opencode.json
windsurf.mcp.json
clippy.txt
clippy.json
clippy_results.json
clippy_unwrap.json
clippy_unwrap_clean.txt
check_output.txt
.next_backup*/
storage.sqlite3
storage.sqlite3-shm
storage.sqlite3-wal
storage.sqlite3.bak
storage.sqlite3.corrupt-*
storage.sqlite3-shm.corrupt-*
storage.sqlite3-wal.corrupt-*

# NTM session state
.ntm/

# Codex CLI cache
.codex

# csplit scratch output at repo root
/xx[0-9][0-9]

# Agent scratch git diff/log/show captures and man-page dumps
.git-*.txt
.git_*.txt
.gemini_*.txt
.man-*.txt
.man_*.txt
.run_diff.sh
git_recent.txt
git_log.txt
git_log_*.txt
git_show_*.txt
