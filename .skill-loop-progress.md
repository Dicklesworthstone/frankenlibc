# Repeated Skill Loop Progress

Objective: apply extreme-software-optimization repeatedly to `bd-tti4cb` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/HOTSPOT_TABLE.md`.

Pass 1 - Profile triage and target scoring: complete.
- Target: `ValidationPipeline::validate` fast-path gating in `crates/frankenlibc-membrane/src/ptr_validator.rs`.
- Evidence: profiler handoff reports `validate_null` about 2131 ns p50 and `validate_known` about 2095 ns p50 while isolated stages are <=13 ns.
- Candidate lever: run dependency-forced null, TLS-cache-hit, and empty-page-oracle exits before the runtime-feedback gate when validation logging is disabled.
- Score: Impact 5 x Confidence 4 / Effort 2 = 10.0.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench membrane_bench validate_`.
- Baseline p50 strict: `validate_null=3095.022 ns`, `validate_known=3060.201 ns`, `validate_foreign=1609.716 ns`, `validate_foreign_nonempty_oracle=1584.457 ns`.
- Golden command: `RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-membrane --lib ptr_validator::tests::default_cache_hit_fast_path_matches_logged_pipeline_hit -- --exact --nocapture`.
- Golden transcript sha256: `5dcc160709f115dc62a68e2ed7ea3e099451d96174aa3ea657b9c17706b9b80f`.
- Golden normalized exact-test-line sha256: `cc4b1a202746214e8b85c175c337b9203e88fd8326c7ef51de5d35755f5faa14`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Safety-facing outputs are preserved by tests comparing no-log fast exits to logged full-path outcomes for null, TLS-cache, and empty-oracle foreign pointers.
- Full validation ordering/tie-breaking is unchanged: `validate_with_security_context` still owns `check_ordering`, `decide`, and all full-path `observe_validation_result` calls.
- Floating-point and RNG behavior are unaffected: the edited path performs only integer/null/cache/oracle checks and does not touch runtime-math stochastic controllers.
- Logging behavior is preserved: logging enabled still routes through the full trace path; logging disabled emits no validation log rows.

Pass 4 - One-lever implementation: complete.
- Lever: when validation logging is disabled, `ValidationPipeline::validate` uses dependency-forced null, TLS-cache-hit, and empty-page-oracle exits before trace construction/runtime feedback.
- Full validation ordering, runtime decisions, and feedback remain on the existing runtime-math pipeline for non-fast-path validations.
- Note: a feedback-preserving variant was measured and rejected because it did not clear the score gate on same-worker rch criterion.

Pass 5 - Re-benchmark and validation: complete.
- After command: `RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench membrane_bench -- validate_ --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- After p50 strict: `validate_null=11.220 ns`, `validate_known=19.528 ns`, `validate_foreign=18.602 ns`, `validate_foreign_nonempty_oracle=1362.349 ns`.
- Before/after p50 vs current-HEAD rch baseline: `validate_null 3008.069 -> 11.220 ns`, `validate_known 3078.129 -> 19.528 ns`, `validate_foreign 1623.562 -> 18.602 ns`.
- Confirmation command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench membrane_bench validate_`.
- Confirmation p50 strict: `validate_null=10.002 ns`, `validate_known=17.055 ns`, `validate_foreign=16.911 ns`, `validate_foreign_nonempty_oracle=1091.712 ns`.
- Behavior proof: focused rch tests compare fast no-log outcomes to logged full-path outcomes for null, TLS-cache-hit, and empty-oracle foreign pointers; final run passed 3/3.
- Golden sha256 stable: `tests/conformance/tls_cache_epoch_invalidation.golden.json = 53ede3d727fbf061d608bf50c10b2faa9873716eeb517e9ca1276edbc3e881c1`.
- Golden normalized exact-test-line sha256 matched baseline: `cc4b1a202746214e8b85c175c337b9203e88fd8326c7ef51de5d35755f5faa14`.
- Validation: `cargo test -p frankenlibc-membrane --lib ptr_validator::tests::default_ -- --nocapture`, `cargo check -p frankenlibc-membrane --all-targets`, and `cargo clippy -p frankenlibc-membrane --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-membrane` passed locally because rch treats fmt as non-compilation.
- UBS: scanned `crates/frankenlibc-membrane/src/ptr_validator.rs`; nonzero due pre-existing test-only panic/unwrap/assert inventory, with no new critical finding in the edited fast-path production code.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization repeatedly to `bd-6ypsli` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/string_bench.summary.md`.

Pass 1 - Profile triage and target scoring: complete.
- Target: `frankenlibc_core::string::mem::memcmp` in `crates/frankenlibc-core/src/string/mem.rs`.
- Evidence: profiler handoff reports equal 4096-byte `memcmp` at `1255 ns` while same-suite `memcpy` is `31.6 ns`; fresh rch baseline measured `1593.221 ns` p50 for 4096 bytes.
- Candidate lever: compare fixed 8-byte chunks for equality, falling back to byte-order comparison only inside a differing chunk.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench memcmp -- --sample-size 40`.
- Baseline p50 raw: `memcmp_16=7.759 ns`, `memcmp_64=26.353 ns`, `memcmp_256=108.320 ns`, `memcmp_1024=402.212 ns`, `memcmp_4096=1593.221 ns`.
- Golden command: `RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core memcmp -- --nocapture`.
- Golden normalized existing-test-line sha256: `eef1f26220f1736c43e59f193a2b8b1fb9a8ecf5dbb2a5abf2fb31071ac18312`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering is preserved because chunk words are used only for equality rejection; first differing byte and unsigned tie-break are still computed by byte scan in original order.
- Partial-length behavior is preserved by slicing both inputs to `min(n, a.len(), b.len())` before chunking.
- Floating-point and RNG behavior are unaffected: the edited function performs only integer byte comparisons and does not touch global state.
- Added a targeted regression for first-difference ordering inside an 8-byte bulk chunk and unsigned tail ordering.

Pass 4 - One-lever implementation: complete.
- Lever: safe Rust 8-byte chunk equality with byte fallback on chunk mismatch and tail bytes.
- No unsafe reads, no endian ordering shortcut, no heap allocation, and no benchmark harness changes.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench memcmp -- --sample-size 40`.
- Post p50 raw: `memcmp_16=2.917 ns`, `memcmp_64=5.842 ns`, `memcmp_256=15.859 ns`, `memcmp_1024=62.250 ns`, `memcmp_4096=208.501 ns`.
- Before/after p50 raw: `memcmp_16 7.759 -> 2.917 ns`, `memcmp_64 26.353 -> 5.842 ns`, `memcmp_256 108.320 -> 15.859 ns`, `memcmp_1024 402.212 -> 62.250 ns`, `memcmp_4096 1593.221 -> 208.501 ns`.
- Golden normalized existing-test-line sha256 matched baseline: `eef1f26220f1736c43e59f193a2b8b1fb9a8ecf5dbb2a5abf2fb31071ac18312`; full post-test-line sha256 including the new regression is `2de28db68bbcf8edcaab17df8dad9f9e63915076fa6528c41ce1f78b1b388fd4`.
- Validation: `cargo test -p frankenlibc-core memcmp -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core`, `git diff --check`, and `ubs crates/frankenlibc-core/src/string/mem.rs` passed locally.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization repeatedly to `bd-wvxyzs` using the profiler handoff for `runtime_math::design::OptimalDesignController::choose_plan`.

Pass 1 - Profile triage and target scoring: complete.
- Target: `OptimalDesignController::choose_plan` in `crates/frankenlibc-membrane/src/runtime_math/design.rs`.
- Evidence: bead profiler handoff reports `design_choose_plan` about `1008.7 ns` p50 with root cause at `design.rs:137`: copy the Fisher matrix, apply a rank-one update, and recompute a full Cholesky/logdet for every candidate probe.
- Fresh rch baseline measured `design_choose_plan=1747.061 ns` p50 on worker `ts2`.
- Candidate lever: compute the baseline Cholesky once and score rank-one candidate gains with the matrix determinant lemma, falling back to the old full-logdet path if the matrix is not SPD.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench runtime_math_kernels_bench design_choose_plan -- --sample-size 50`.
- Baseline p50 strict: `design_choose_plan=1747.061 ns` (`p95=1845.895 ns`, `p99=1903.000 ns`).
- Golden command: `RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-membrane runtime_math::design -- --nocapture`.
- Golden sorted existing-test-line sha256: `32b0990b201b4404c2bab4c093c51badd5f4009a646a41c9b0dd9c3ee96080b3`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering and tie-breaking are preserved because candidate order, `sort_by` tie handling, budget pruning, fallback probe insertion, and record/update paths are unchanged.
- Floating-point semantics are equivalent for SPD Fisher matrices by `det(A + wvv^T) = det(A) * (1 + w * v^T A^-1 v)`, using the same Cholesky factorization already used by `logdet_spd`.
- Non-SPD behavior is preserved by falling back to the previous full-copy `rank_one_update` plus full `logdet_spd` candidate scoring path.
- RNG and external state are unaffected: the edit only changes deterministic matrix scoring inside `choose_plan`.

Pass 4 - One-lever implementation: complete.
- Lever: factor the current Fisher matrix once, reuse the Cholesky factor for per-probe rank-one logdet gains, and keep the old full-logdet path as the non-SPD fallback.
- Added a regression test that compares the determinant-lemma gain against the full matrix-copy/update/logdet computation for every probe.
- No benchmark harness changes and no changes to probe costs, budgets, risk thresholds, mask handling, or plan construction.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench runtime_math_kernels_bench design_choose_plan -- --sample-size 50`.
- Post p50 strict: `design_choose_plan=432.283 ns` (`p95=490.500 ns`, `p99=607.469 ns`).
- Before/after p50 strict: `design_choose_plan 1747.061 -> 432.283 ns`.
- Golden sorted existing-test-line sha256 matched baseline: `32b0990b201b4404c2bab4c093c51badd5f4009a646a41c9b0dd9c3ee96080b3`; full post-test-line sha256 including the new regression is `e3bed02139b6cc5502c48f3a9384feb5477641cba629ac4d1e4e11965b3f14db`.
- Validation: `cargo test -p frankenlibc-membrane runtime_math::design -- --nocapture`, `cargo check -p frankenlibc-membrane --all-targets`, and `cargo clippy -p frankenlibc-membrane --all-targets -- -D warnings` all passed via rch after the final edit; `cargo fmt --check -p frankenlibc-membrane` and `git diff --check` passed locally because rch treats fmt as non-compilation.
- UBS: scanned `crates/frankenlibc-membrane/src/runtime_math/design.rs`; no critical findings. The new test was adjusted to avoid adding an `expect` warning.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization to `bd-yftnsz` using the profiler handoff for `stdio::printf::parse_format_string`.

Pass 1 - Profile triage and target scoring: complete.
- Target: `parse_format_string` in `crates/frankenlibc-core/src/stdio/printf.rs`.
- Evidence: bead profiler handoff reports `stdio_printf_parse/literal=26.8 ns` for a percent-free literal format, with root cause identified as per-call owned `Vec<FormatSegment>` allocation.
- Fresh rch baseline measured `literal=40.542 ns`, `mixed=169.74 ns`, `positional=153.32 ns` on worker `ts2`.
- Candidate lever: preserve the existing parser but replace the returned `Vec` with a small segment container that avoids heap allocation for one-segment formats and falls back to an 8-capacity `Vec` for multi-segment formats.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench stdio_bench -- stdio_printf_parse --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- Baseline p50 raw: `literal=40.542 ns`, `mixed=169.74 ns`, `positional=153.32 ns`.
- Golden sha256: `tests/conformance/fixtures/printf_conformance.json=b8657a70042071e59636fe167d7ffdfb6ae25dab77a173056cec1465ae27c6ad`, `tests/conformance/printf_star_width_precision_completion_contract.v1.json=8d95e8578dc4234246b5537dcd99fbe1c865caa3059c695aa595491d2272c4ed`, `tests/conformance/stdio_printf_overflow_integration_completion_contract.v1.json=f9a3136fe8fadc05be40e89bd6025d60f1d4e2626dfb98d7f102b1fd112ac3c8`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering and tie-breaking are preserved because the byte parser loop, `parse_format_spec`, segment push order, positional width/precision handling, and render/count consumers are unchanged.
- Floating-point and RNG behavior are unaffected: the edit only changes segment container storage and does not change formatting math, argument extraction order, global state, or random sources.
- Public caller behavior is preserved by `Deref<Target=[FormatSegment]>`, borrowed iteration, and owned iteration compatibility for existing `&segments`, indexing, `.len()`, and `for seg in parse_format_string(...)` callers.

Pass 4 - One-lever implementation: complete.
- Lever: `FormatSegments` stores zero or one segment inline and falls back to a `Vec` with capacity 8 on the second segment; `FormatSpec` and `FormatSegment` are `Copy` to support safe owned iteration without cloning heap data.
- No benchmark harness changes, no global parse cache, no mutex, no parser grammar changes, and no ABI argument extraction changes.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench stdio_bench -- stdio_printf_parse --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- Post p50 raw: `literal=18.997 ns`, `mixed=94.683 ns`, `positional=99.395 ns`.
- Before/after p50 raw: `literal 40.542 -> 18.997 ns`, `mixed 169.74 -> 94.683 ns`, `positional 153.32 -> 99.395 ns`.
- Golden sha256 unchanged for the three printf conformance/contract fixtures listed above.
- Validation: `cargo test -p frankenlibc-core printf -- --nocapture`, `cargo test -p frankenlibc-core fmtcheck -- --nocapture`, `cargo check -p frankenlibc_conformance --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check` passed locally because rch treats fmt as non-compilation.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization to `bd-osbo8c` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/resolv_parsers_bench.summary.md`.

Pass 1 - Profile triage and target scoring: complete.
- Started: 2026-06-01T20:41:45-04:00.
- Target: `extract_triples_into` in `crates/frankenlibc-core/src/netgroup/mod.rs`.
- Evidence: profiler handoff reports `parse_netgroup_triples_match` at 603.0 ns p50 / 612.7 ns mean on rch AMD EPYC, with source-confirmed `Vec<&[u8]>` allocation per parsed triple.
- Candidate lever: replace the throwaway collected split parts with a split iterator that reads only host/user/domain fields.
- Score: Impact 3 x Confidence 5 / Effort 1 = 15.0.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench resolv_parsers_bench`.
- Baseline p50 raw: `parse_netgroup_triples_match=625.374 ns` (`p95=735.542 ns`, `p99=770.599 ns`, worker `vmi1156319`).
- Golden command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core netgroup -- --nocapture`.
- Golden netgroup test-line sha256: `379ccb0b102eb727a6f7fa2568c6c4727acc492c5018ff21a89a3554a84c6a9d`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering is preserved because the parser still scans triples left-to-right and fields left-to-right, reading host, user, then domain from the same comma split.
- Tie-breaking is not applicable: netgroup parsing emits every matching triple and performs no ordering comparisons beyond case-insensitive group-name equality.
- Floating-point and RNG behavior are unaffected: the edited path only slices bytes and allocates the same owned output fields.
- Missing-field, empty-field, whitespace trimming, comments, and unclosed-paren behavior remain covered by the existing netgroup unit tests.

Pass 4 - One-lever implementation: complete.
- Lever: replaced the per-triple `Vec<&[u8]>` collection with a split iterator that reads only the first three fields.
- No benchmark harness changes, no parser grammar changes, no output ownership changes, and no changes outside `netgroup/mod.rs`.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench resolv_parsers_bench`.
- Post p50 raw: `parse_netgroup_triples_match=552.258 ns` (`p95=609.747 ns`, `p99=683.643 ns`, worker `ts2`).
- Before/after p50 raw: `parse_netgroup_triples_match 625.374 -> 552.258 ns`; scheduler changed workers, so confidence is based on the measured win plus the source-confirmed allocation removal.
- Golden netgroup test-line sha256 matched baseline: `379ccb0b102eb727a6f7fa2568c6c4727acc492c5018ff21a89a3554a84c6a9d`.
- Validation: `cargo test -p frankenlibc-core netgroup -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core`, `git diff --check`, and `ubs crates/frankenlibc-core/src/netgroup/mod.rs` passed locally with no critical findings.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0.

---

Objective: apply extreme-software-optimization to `bd-yj1zlr` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/string_bench.summary.md`.

Pass 1 - Measurement coverage: complete.
- Started: 2026-06-01T20:56:08-04:00.
- Target: `strcspn`, `strspn`, and `strpbrk` general arms in `crates/frankenlibc-core/src/string/str.rs`.
- Evidence gap: handoff identifies O(n*m) `set.contains` for set length >=4, but current string bench only covers size-1 accept/reject sets.
- Measurement lever: add >=4-byte set benchmark groups for `strcspn` absent, `strpbrk` absent, and `strspn` full-scan before changing production code.

Pass 2 - Baseline: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench general -- --sample-size 50`.
- Baseline p50 raw: `strcspn_general_absent_4096=9238.067 ns`, `strpbrk_general_absent_4096=9036.853 ns`, `strspn_general_full_4096=3159.075 ns` on worker `ts2`.
- Decision: proceed to production optimization because the measured general-arm scans are far slower than the same-suite small-set fast paths and match the O(n*m) profiler handoff.

Pass 3 - Golden behavior and isomorphism proof: complete.
- Pre/post golden commands: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core strcspn -- --nocapture`, plus the same command for `strpbrk` and `strspn`.
- Golden test-line sha256 matched pre/post: `strcspn=714e609165d795257e5d56ce4d4b6005e2f04621ff05f6039d31968655a211a0`, `strpbrk=9ceafdce3d1c2e2c5361395794f682fbfc7f640262b09abea87cd12dc30bdb56`, `strspn=047b45befc1af165b4398c16dad98d389225586fb1db9aa37f590c717143fb88`.
- Ordering and tie-breaking are preserved because all three functions still scan `s` left-to-right and return the first byte whose membership status changes the result.
- Duplicate accept/reject bytes remain duplicate-insensitive because `strspn`, `strcspn`, and `strpbrk` define set membership, not multiplicity.
- Floating-point and RNG behavior are unaffected: the edited code only builds a local byte-membership table and performs byte indexing.

Pass 4 - One-lever implementation: complete.
- Lever: the existing general `>=4` accept/reject arms now build a local `[bool; 256]` membership table once, replacing per-byte linear `slice.contains` checks.
- The 0/1/2/3-byte fast paths, NUL handling, returned indexes, and `None` behavior are unchanged.
- No ABI changes, no caching/global state, no benchmark scoring changes, and no edits to unrelated string routines.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench general -- --sample-size 50`.
- Before/after p50 raw on `ts2`: `strcspn_general_absent_4096 9238.067 -> 2418.668 ns`, `strpbrk_general_absent_4096 9036.853 -> 2386.458 ns`, `strspn_general_full_4096 3159.075 -> 2633.772 ns`.
- Validation: `cargo test -p frankenlibc-core strcspn -- --nocapture`, `cargo test -p frankenlibc-core strpbrk -- --nocapture`, `cargo test -p frankenlibc-core strspn -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, `cargo check -p frankenlibc-bench --all-targets`, `cargo clippy -p frankenlibc-core --all-targets -- -D warnings`, and `cargo clippy -p frankenlibc-bench --bench string_bench -- -D warnings` all passed via rch.
- `cargo clippy -p frankenlibc-bench --all-targets -- -D warnings` is blocked by unrelated pre-existing `manual_is_multiple_of` in `strict_hardened_overhead_harness.rs` from `bd-wpr1n`; not part of this bead or reservation.
- Local checks: `cargo fmt --check -p frankenlibc-core -p frankenlibc-bench`, `git diff --check`, and `ubs crates/frankenlibc-core/src/string/str.rs crates/frankenlibc-bench/benches/string_bench.rs` passed; UBS reported 0 critical findings.
- Score: Impact 5 x Confidence 5 / Effort 1 = 25.0.

---

Objective: apply extreme-software-optimization to `bd-2dsf3y` and `bd-6lwqp8` after `bd-qe3hmn` shifted the queue.

Pass 1 - Runtime-math profile triage and rejected lever: complete.
- Started: 2026-06-02T02:32:22-04:00.
- No ready/open perf beads existed after `bd-qe3hmn`; fresh rch profile selected the remaining representative runtime-math feedback target.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_MODE=strict cargo bench -p frankenlibc-bench --bench runtime_math_bench decide_observe -- --noplot`.
- Baseline on `vmi1149989`: `decide_observe` p50 `923.343 ns`, p95 `1169.875 ns`, p99 `1419.978 ns`.
- Rejected lever: removing the `#[cold]` / `#[inline(never)]` boundary from `observe_validation_result_enabled`.
- Rejected after-run on `vmi1153651`: p50 `1962.151 ns`, p95 `8184.733 ns`, p99 `11809.962 ns`; source attributes restored manually, no code kept.

Pass 2 - String target triage and baseline: complete.
- Target: `bd-6lwqp8`, size-1 `strsep` delimiter scans in `crates/frankenlibc-core/src/string/str.rs`.
- Evidence: profiler handoff reported `strsep_absent_4096=3224.7 ns` p50; earlier string beads had closed `strchr`, `strrchr`, `strcspn`, `strpbrk`, and `strspn`, but `strsep` still used `delim_set.contains(&byte)`.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strsep_absent -- --sample-size 50 --noplot`.
- Baseline p50 raw on `vmi1227854`: `strsep_absent_16=18.837 ns`, `64=60.359 ns`, `256=222.369 ns`, `1024=804.979 ns`, `4096=3193.848 ns`.
- Candidate score: Impact 5 x Confidence 5 / Effort 1 = 25.0.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering is preserved because the one-byte fast path calls the existing target-or-NUL scanner, which returns the first delimiter or first NUL, then mutates only when the delimiter won that tie.
- Empty delimiter behavior is preserved by returning `None` when `strlen(delim) == 0`; this matches the previous empty-set membership scan.
- NUL termination and absent-delimiter behavior are preserved because `find_byte_or_nul` stops at the first NUL and returns `None` when the delimiter is absent before it.
- Multi-byte delimiter behavior is unchanged; it still uses the existing membership scan.
- Floating-point and RNG behavior are unaffected: the edited path only performs integer byte/word comparisons over the supplied slice.

Pass 4 - One-lever implementation: complete.
- Lever: special-case `delim_len == 1` in `strsep` and route it through `find_byte_or_nul`, then write the delimiter byte to NUL only on a real delimiter match.
- Added `test_strsep_single_byte_delimiter_bulk_scan_mutates_only_match` to pin mutation boundaries for the bulk-scan path.
- No benchmark harness changes, no global state, no multi-byte delimiter changes.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strsep_absent -- --sample-size 50 --noplot`.
- Post p50 raw on `vmi1149989`: `strsep_absent_16=8.310 ns`, `64=13.030 ns`, `256=29.656 ns`, `1024=104.178 ns`, `4096=436.585 ns`.
- Before/after p50 raw: `strsep_absent_16 18.837 -> 8.310 ns`, `64 60.359 -> 13.030 ns`, `256 222.369 -> 29.656 ns`, `1024 804.979 -> 104.178 ns`, `4096 3193.848 -> 436.585 ns`.
- Golden strsep test-status sha256: `e2752218a0b0b6654acdd1b3c6d7c413cee968d10fcfe465db8bd21bac6c86b8`.
- Source sha256 after patch: `str.rs=de436d5fcfee534b73ec864ba7eb8b27a569c2814c3a79679031abc010d6aac8`.
- Validation: `cargo test -p frankenlibc-core strsep -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core` and `git diff --check` passed locally.
- Score: Impact 5 x Confidence 5 / Effort 1 = 25.0.

---

Objective: apply extreme-software-optimization to `bd-h0ii93` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/string_bench.summary.md`.

Pass 1 - Profile triage and target scoring: complete.
- Started: 2026-06-01T21:17:00-04:00.
- Target: `strchr` and `strchrnul` forward scans in `crates/frankenlibc-core/src/string/str.rs`.
- Evidence: profiler handoff reports absent 4096-byte `strchr` near 1496 ns and `strchrnul` near 1562 ns while same-suite `memcpy` is 31.6 ns; fresh rch baseline measured `strchr_absent_4096=1681.243 ns` and `strchrnul_absent_4096=1801.781 ns`.
- Candidate lever: replace byte-at-a-time target-or-NUL scanning with aligned word-at-a-time detection, falling back to byte order only inside the first word that may contain the target or terminator.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strchr -- --sample-size 50`.
- Baseline p50 raw on worker `vmi1152480`: `strchr_absent_16=7.901 ns`, `strchr_absent_64=24.456 ns`, `strchr_absent_256=132.407 ns`, `strchr_absent_1024=455.500 ns`, `strchr_absent_4096=1681.243 ns`, `strchrnul_absent_16=9.442 ns`, `strchrnul_absent_64=27.120 ns`, `strchrnul_absent_256=139.749 ns`, `strchrnul_absent_1024=453.349 ns`, `strchrnul_absent_4096=1801.781 ns`.
- Golden command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core strchr -- --nocapture`.
- Golden strchr test-line sha256: `302bc54dfb89df8887a7236dcf3884c41a5ca80f78e2fe95580f3f9488321084`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering is preserved because word matches are used only as a coarse "contains target or NUL" detector; the final return still comes from the original left-to-right byte scan inside the first matching word.
- Tie-breaking is preserved because target and NUL in the same word are resolved by byte order, so `strchr` stops at the first NUL before a later target and returns a target before a later NUL.
- Unterminated slice behavior is preserved: `strchr(s, 0)` still returns `Some(s.len())`, non-NUL `strchr` still returns `None`, and `strchrnul` still returns `s.len()` when neither target nor NUL appears.
- Floating-point and RNG behavior are unaffected: the edited path only performs integer byte/word comparisons and reads from the supplied slice.

Pass 4 - One-lever implementation: complete.
- Lever: added a shared `find_byte_or_nul` helper with alignment peel, usize word target-or-NUL detection, and byte fallback for the candidate word/tail.
- No benchmark harness changes, no global state, no changes to reverse scans, and no changes to other string routines beyond sharing the existing NUL-detection idiom.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strchr -- --sample-size 50`.
- Before/after p50 raw on worker `vmi1152480`: `strchr_absent_16 7.901 -> 3.144 ns`, `strchr_absent_64 24.456 -> 8.784 ns`, `strchr_absent_256 132.407 -> 30.096 ns`, `strchr_absent_1024 455.500 -> 124.923 ns`, `strchr_absent_4096 1681.243 -> 501.548 ns`, `strchrnul_absent_16 9.442 -> 2.943 ns`, `strchrnul_absent_64 27.120 -> 7.657 ns`, `strchrnul_absent_256 139.749 -> 30.348 ns`, `strchrnul_absent_1024 453.349 -> 108.875 ns`, `strchrnul_absent_4096 1801.781 -> 452.296 ns`.
- Golden strchr test-line sha256 matched baseline: `302bc54dfb89df8887a7236dcf3884c41a5ca80f78e2fe95580f3f9488321084`.
- Validation: `cargo test -p frankenlibc-core strchr -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core`, `git diff --check`, and `ubs crates/frankenlibc-core/src/string/str.rs` passed locally.
- UBS reported 0 critical findings; warnings are existing broad inventory around tests, assertions, and indexing in the file.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization to `bd-fslmjp` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/string_bench.summary.md`.

Pass 1 - Profile triage and target scoring: complete.
- Started: 2026-06-01T22:07:00-04:00.
- Target: size-1 `strcspn` and `strpbrk` set scans in `crates/frankenlibc-core/src/string/str.rs`.
- Evidence: fresh rch baseline measured `strcspn_absent_4096=2504.701 ns` and `strpbrk_absent_4096=2218.437 ns` with byte-at-a-time loops in the single-byte accept/reject arms.
- Candidate lever: route the size-1 arms through the existing word-at-a-time target-or-NUL scanner added for forward string searches.
- Score: Impact 5 x Confidence 5 / Effort 1 = 25.0.

Pass 2 - Baseline and golden behavior: complete.
- Baseline commands: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strcspn_absent -- --sample-size 50` and the matching `strpbrk_absent` command.
- Baseline p50 raw: `strcspn_absent_16=19.434 ns`, `strcspn_absent_64=52.562 ns`, `strcspn_absent_256=229.272 ns`, `strcspn_absent_1024=674.865 ns`, `strcspn_absent_4096=2504.701 ns`.
- Baseline p50 raw: `strpbrk_absent_16=9.479 ns`, `strpbrk_absent_64=49.388 ns`, `strpbrk_absent_256=151.143 ns`, `strpbrk_absent_1024=507.641 ns`, `strpbrk_absent_4096=2218.437 ns`.
- Golden commands: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core strcspn -- --nocapture` and the matching `strpbrk` command.
- Golden test-line sha256: `strcspn=714e609165d795257e5d56ce4d4b6005e2f04621ff05f6039d31968655a211a0`, `strpbrk=9ceafdce3d1c2e2c5361395794f682fbfc7f640262b09abea87cd12dc30bdb56`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering is preserved because `find_byte_or_nul` only detects candidate words; the final match inside the first candidate word is resolved left-to-right by byte index.
- Tie-breaking is preserved because a NUL before the accepted/rejected byte still terminates the scan first, and a matching byte before NUL still wins.
- Unterminated slice behavior is preserved because no candidate word or tail match returns `s.len()` for `strcspn` and `None` for `strpbrk`.
- Floating-point and RNG behavior are unaffected: the edited path only performs integer byte/word comparisons over the supplied slice.

Pass 4 - One-lever implementation: complete.
- Lever: size-1 `strcspn` returns `find_byte_or_nul(s, rejected)`, and size-1 `strpbrk` uses the same index but returns `Some(index)` only when the byte at that index is the accepted byte.
- No benchmark harness changes, no global state, no changes to general set membership arms, and no changes to the shared word scanner itself.

Pass 5 - Re-benchmark and validation: complete.
- Post commands: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strcspn_absent -- --sample-size 50` and the matching `strpbrk_absent` command.
- Post p50 raw: `strcspn_absent_16=7.852 ns`, `strcspn_absent_64=13.524 ns`, `strcspn_absent_256=39.095 ns`, `strcspn_absent_1024=124.486 ns`, `strcspn_absent_4096=528.256 ns`.
- Post p50 raw: `strpbrk_absent_16=3.741 ns`, `strpbrk_absent_64=8.401 ns`, `strpbrk_absent_256=22.581 ns`, `strpbrk_absent_1024=87.126 ns`, `strpbrk_absent_4096=345.128 ns`.
- Before/after p50 raw: `strcspn_absent_4096 2504.701 -> 528.256 ns`, `strpbrk_absent_4096 2218.437 -> 345.128 ns`.
- Golden test-line sha256 matched baseline: `strcspn=714e609165d795257e5d56ce4d4b6005e2f04621ff05f6039d31968655a211a0`, `strpbrk=9ceafdce3d1c2e2c5361395794f682fbfc7f640262b09abea87cd12dc30bdb56`.
- Validation: `cargo test -p frankenlibc-core strcspn -- --nocapture`, `cargo test -p frankenlibc-core strpbrk -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core`, `git diff --check`, and `ubs crates/frankenlibc-core/src/string/str.rs` passed locally.
- UBS reported 0 critical findings; warnings are existing broad inventory around tests, assertions, and indexing in the file.
- Score: Impact 5 x Confidence 5 / Effort 1 = 25.0.

---

Objective: apply extreme-software-optimization to `bd-qe3hmn` using the corrected profiler handoff for strict/hardened overhead measurement attribution.

Pass 1 - Measurement attribution and source-of-truth split: complete.
- Started: 2026-06-02T05:43:10Z.
- Target: `crates/frankenlibc-bench/benches/runtime_math_kernels_bench.rs` and `crates/frankenlibc-bench/benches/strict_hardened_overhead_harness.rs`.
- Evidence: the old overhead smoke harness reproduced the cadence-inclusive floor on `vmi1149989`: strict `ctype=87 ns` p50, strict `runtime_math=86 ns` p50, hardened `runtime_math=72 ns` p50.
- Existing Criterion resolution check on `vmi1153651`: `risk_upper_bound_ppm=0.556 ns` p50, proving the Criterion lane can resolve sub-100ns rows.
- Lever: add dedicated Criterion rows for `RuntimeMathKernel::decide`: `runtime_math_decide_resample_free` and `runtime_math_decide_cadence_inclusive`; leave `strict_hardened_overhead_harness` as cadence-inclusive cross-mode/family ranking evidence.
- Rejected approaches not repeated: paired representative-workload subtraction and paired context-construction subtraction, both previously failed to improve measurement clarity.
- Strict after numbers on `vmi1153651`: resample-free p50 `190.360 ns`, cadence-inclusive p50 `194.420 ns`.
- Hardened after numbers on `vmi1153651`: resample-free p50 `188.393 ns`, cadence-inclusive p50 `199.565 ns`.
- Isomorphism proof: no production libc behavior changes; ordering/tie-breaking are unchanged because only benchmark contexts and labels were added; floating-point and RNG behavior are not touched.
- Golden/source sha256 after patch: `runtime_math_kernels_bench.rs=b49935a47b2e1e1ffa05001b1372e7fc009e3a70b886b639ab4c06a87394766b`, `strict_hardened_overhead_harness.rs=e9c53196bb0641f87f9e3cc423247a681694d075ca7e01886649c5e81e9834fc`.
- Validation: `cargo check -p frankenlibc-bench --benches`, `cargo clippy -p frankenlibc-bench --benches -- -D warnings`, and `cargo test -p frankenlibc-bench` all passed via rch; `cargo fmt -p frankenlibc-bench --check` and `git diff --check` passed locally because rch refuses remote fmt as non-compilation.
- Score: Impact 4 x Confidence 5 / Effort 2 = 10.0.

---

Objective: apply extreme-software-optimization to `bd-je4r5l` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/string_bench.summary.md`.

Pass 1 - Profile triage and target scoring: complete.
- Started: 2026-06-01T21:43:48-04:00.
- Target: `strrchr` reverse search in `crates/frankenlibc-core/src/string/str.rs`.
- Evidence: profiler handoff reports `strrchr_absent_4096=2538.6 ns` p50 (0.620 ns/byte), the slowest untouched byte-string scalar scan in the artifact.
- Candidate lever: locate the effective terminator with the existing word-at-a-time `strlen`, then scan the prefix backward word-at-a-time for the target byte.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strrchr_absent -- --sample-size 50`.
- Baseline p50 raw on worker `vmi1152480`: `strrchr_absent_16=8.686 ns`, `strrchr_absent_64=34.270 ns`, `strrchr_absent_256=170.741 ns`, `strrchr_absent_1024=637.568 ns`, `strrchr_absent_4096=2511.232 ns`.
- Golden command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core strrchr -- --nocapture`.
- Existing strrchr test-line sha256: `ec55f3e0bf6c4e34cc7ffbb8f84162f4882828bea6433e5b6905a77d0e39f2e0`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering is preserved because the backward word scan only identifies candidate words; the final match inside a candidate word is resolved byte-by-byte from high index to low index.
- Stop-at-NUL behavior is preserved because the scan boundary is `strlen(s)`, so bytes after the first NUL are ignored.
- `c == 0` behavior is preserved because `strrchr(s, 0)` returns the first terminator index, or `s.len()` for unterminated slices, exactly as before.
- Floating-point and RNG behavior are unaffected: the edited path only performs integer byte/word comparisons over the supplied slice.

Pass 4 - One-lever implementation: complete.
- Lever: `strrchr` now uses `strlen(s)` for the effective string length and a new `find_last_byte_before` helper with alignment peel, usize word target detection, and reverse byte fallback in the candidate word.
- Added a regression proving the last target before the terminator wins and a target after NUL is ignored.
- No benchmark harness changes, no global state, and no changes to substring/search routines other than `strrchr`.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strrchr_absent -- --sample-size 50`.
- Post p50 raw on worker `ts2`: `strrchr_absent_16=9.212 ns`, `strrchr_absent_64=20.369 ns`, `strrchr_absent_256=56.405 ns`, `strrchr_absent_1024=219.726 ns`, `strrchr_absent_4096=810.207 ns`.
- Before/after p50 raw: `strrchr_absent_64 34.270 -> 20.369 ns`, `strrchr_absent_256 170.741 -> 56.405 ns`, `strrchr_absent_1024 637.568 -> 219.726 ns`, `strrchr_absent_4096 2511.232 -> 810.207 ns`; 16-byte case is neutral/regressed within ns-scale noise (`8.686 -> 9.212 ns`) while the profiled long-scan target wins 3.1x.
- Existing strrchr test-line sha256 matched baseline: `ec55f3e0bf6c4e34cc7ffbb8f84162f4882828bea6433e5b6905a77d0e39f2e0`; full post-test-line sha256 including the new regression is `27dff5efe88c5fe0d06ca535f476f6c7bdbe01aae3f255bde68af10b96ec2789`.
- Validation: `cargo test -p frankenlibc-core strrchr -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core`, `git diff --check`, and `ubs crates/frankenlibc-core/src/string/str.rs` passed locally.
- UBS reported 0 critical findings; warnings are existing broad inventory around tests, assertions, and indexing in the file.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization to `bd-perf-strspn-single-byte-2v81z2` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/string_bench.summary.md`.

Pass 1 - Profile triage and target scoring: complete.
- Started: 2026-06-01T22:38:28-04:00.
- Target: size-1 `strspn` full scans in `crates/frankenlibc-core/src/string/str.rs`.
- Evidence: profiler handoff reports `strspn_full=2568.6 ns` p50 at 4096B; fresh rch baseline measured `strspn_full_4096=4372.542 ns` p50 with the size-1 accept arm scanning byte-by-byte.
- Candidate lever: skip aligned words that are entirely the accepted byte, then resolve the first candidate word byte-by-byte to preserve NUL and first-nonaccepted ordering.
- Score: Impact 5 x Confidence 5 / Effort 1 = 25.0.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strspn_full -- --sample-size 50`.
- Baseline p50 raw on worker `vmi1156319`: `strspn_full_16=21.835 ns`, `strspn_full_64=72.511 ns`, `strspn_full_256=268.938 ns`, `strspn_full_1024=999.109 ns`, `strspn_full_4096=4372.542 ns`.
- Golden command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core strspn -- --nocapture`.
- Golden strspn test-line sha256: `047b45befc1af165b4398c16dad98d389225586fb1db9aa37f590c717143fb88`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering is preserved because the helper only skips words equal to repeated `accepted`; the first word that may contain NUL or a nonaccepted byte is resolved left-to-right by byte index.
- NUL handling is preserved because `accepted` is nonzero when `strlen(accept) == 1`, so any NUL byte makes the word differ from repeated `accepted` and is then found by the byte fallback.
- Unterminated full-slice behavior is preserved because a slice containing only `accepted` bytes reaches the same `s.len()` return.
- Floating-point and RNG behavior are unaffected: the edited path only performs integer byte/word comparisons over the supplied slice.

Pass 4 - One-lever implementation: complete.
- Lever: added `find_non_byte_or_nul` and routed only the size-1 `strspn` arm through it.
- No benchmark harness changes, no global state, no changes to size-2/size-3/general membership arms, and no changes to other string routines.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strspn_full -- --sample-size 50`.
- Post p50 raw on worker `vmi1149989`: `strspn_full_16=3.156 ns`, `strspn_full_64=4.713 ns`, `strspn_full_256=12.406 ns`, `strspn_full_1024=35.406 ns`, `strspn_full_4096=165.661 ns`.
- Before/after p50 raw: `strspn_full_16 21.835 -> 3.156 ns`, `strspn_full_64 72.511 -> 4.713 ns`, `strspn_full_256 268.938 -> 12.406 ns`, `strspn_full_1024 999.109 -> 35.406 ns`, `strspn_full_4096 4372.542 -> 165.661 ns`.
- Golden strspn test-line sha256 matched baseline: `047b45befc1af165b4398c16dad98d389225586fb1db9aa37f590c717143fb88`.
- Validation: `cargo test -p frankenlibc-core strspn -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core`, `git diff --check`, and `ubs crates/frankenlibc-core/src/string/str.rs` passed locally.
- UBS reported 0 critical findings; warnings are existing broad inventory around tests, assertions, and indexing in the file.
- Score: Impact 5 x Confidence 5 / Effort 1 = 25.0.

---

Objective: apply extreme-software-optimization to `bd-grrszn` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/string_bench.summary.md`.

Pass 1 - Profile triage and baseline: complete.
- Started: 2026-06-02T03:01:02-04:00.
- Target: `strcasestr_absent` first-byte casefold scan in `crates/frankenlibc-core/src/string/str.rs`.
- Evidence: profiler handoff reported `strcasestr_absent_4096=2680.4 ns` p50; fresh baseline confirmed `strcasestr_absent_4096=2419.738 ns` p50.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strcasestr_absent -- --sample-size 50 --noplot`.
- Baseline p50 raw on `vmi1149989`: `strcasestr_absent_16=10.324 ns`, `64=50.282 ns`, `256=199.480 ns`, `1024=649.102 ns`, `4096=2419.738 ns`.

Pass 2 - Rejected one-lever probe: complete.
- Candidate lever: precompute the first needle byte's ASCII lower/upper forms and compare the haystack byte directly, avoiding `byte.to_ascii_lowercase()` on first-byte mismatches.
- Intended isomorphism: first-match ordering, NUL stop, unterminated haystack behavior, ASCII-only case-insensitive semantics, and inner match tie-breaking unchanged; FP/RNG untouched.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench strcasestr_absent -- --sample-size 50 --noplot`.
- Post p50 raw on `vmi1227854`: `strcasestr_absent_16=28.747 ns`, `64=102.215 ns`, `256=400.989 ns`, `1024=1563.122 ns`, `4096=5979.075 ns`.
- Decision: rejected. The lever regressed every measured size and the source was restored manually; no code diff kept.

---

Objective: apply extreme-software-optimization to `bd-4c1isf` using the fresh rch string profile after earlier string hotpath fixes.

Pass 1 - Profile triage and target scoring: complete.
- Started: 2026-06-02T03:15:33-04:00.
- Target: exact 4-byte set-membership arms for `strspn`, `strcspn`, and `strpbrk` in `crates/frankenlibc-core/src/string/str.rs`.
- Evidence: fresh full `string_bench` on `vmi1227854` reported remaining top string lanes at `strspn_general_full_4096=3269.632 ns`, `strpbrk_general_absent_4096=3184.701 ns`, and `strcspn_general_absent_4096=2066.086 ns`.
- Candidate lever: add direct 4-byte accept/reject comparisons before the existing 256-entry membership-table path.

Pass 2 - Focused baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench general -- --sample-size 50 --noplot`.
- Focused baseline p50 raw on `vmi1153651`: `strcspn_general_absent_4096=3390.811 ns`, `strpbrk_general_absent_4096=3156.689 ns`, `strspn_general_full_4096=4744.000 ns`.
- Pre-edit affected-test golden sha256: `85a0601c1d08903ccc9c22de2e9046486797541959caf469365aebcdb85bbdcb`.

Pass 3 - Isomorphism proof and edit plan: complete.
- NUL termination and first-match ordering were intended to be preserved because each direct-comparison loop checked bytes in original left-to-right order and stopped before bytes after NUL.
- Duplicate accept/reject bytes were intended to remain duplicate-insensitive because membership was expressed as disjunction over the four bytes, matching set membership.
- Empty-set and 1/2/3-byte paths were untouched; sets larger than four bytes stayed on the existing table path.
- Floating-point and RNG behavior were unaffected: the candidate only changed integer byte comparisons.

Pass 4 - One-lever implementation: rejected.
- Implemented exact 4-byte direct-comparison arms plus focused duplicate/NUL/first-match tests, then ran the same focused rch post-benchmark.
- Post p50 raw on `vmi1153651`: `strcspn_general_absent_4096=5215.178 ns`, `strpbrk_general_absent_4096=5923.216 ns`, `strspn_general_full_4096=4411.075 ns`.
- Decision: rejected. `strcspn` and `strpbrk` regressed heavily, and the `strspn` win was too small to keep the family lever.

Pass 5 - Backout and closeout: complete.
- Source was manually restored; `git diff -- crates/frankenlibc-core/src/string/str.rs` is empty.
- Closed `bd-4c1isf` as no-code-kept with the same before/after numbers.
- Score: below threshold.

---

Objective: apply extreme-software-optimization to `bd-jplosq` using the fresh rch string profile after earlier string hotpath fixes.

Pass 1 - Profile triage and target scoring: complete.
- Started: 2026-06-02T03:31:12-04:00.
- Target: `wcsrchr` non-NUL absent scans in `crates/frankenlibc-core/src/string/wide.rs`.
- Evidence: fresh full `string_bench` on `vmi1227854` reported `wcsrchr_absent_4096=3032.197 ns` p50 after earlier string byte-scan fixes shifted bottlenecks.
- Candidate lever: safe 4-lane forward-scan unroll for `c != 0`, preserving the existing one-pass NUL-bounded last-match semantics.
- Score: Impact 4 x Confidence 5 / Effort 1 = 20.0.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench wcsrchr_absent -- --sample-size 50 --noplot`.
- Focused baseline p50 raw on `vmi1153651`: `wcsrchr_absent_16=19.380 ns`, `64=73.648 ns`, `256=274.002 ns`, `1024=1052.235 ns`, `4096=4464.526 ns`.
- Pre-edit existing `wcsrchr` test-line sha256: `2575c670f2a1afd3210dfbad1abcaf8ea96386a31a5693328afa90eefde43025`.

Pass 3 - Isomorphism proof and edit plan: complete.
- `c == 0` behavior is unchanged because that branch still returns the first terminator index, or `s.len()` for unterminated slices.
- Last-match ordering is preserved because chunks are still inspected in increasing index order and `last` is updated only when a matching element is observed before a terminator.
- NUL-stop behavior is preserved because each element in the unrolled chunk checks for `0` before later lanes are considered.
- Floating-point and RNG behavior are unaffected: the edited path only performs integer comparisons over the supplied `u32` slice.

Pass 4 - One-lever implementation: complete.
- Lever: unrolled the `c != 0` scan four wide characters at a time using safe slice indexing, with the scalar tail loop retained.
- Added `test_wcsrchr_chunk_scan_preserves_last_before_terminator` to pin last-match and after-NUL ignore behavior.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench wcsrchr_absent -- --sample-size 50 --noplot`.
- Post p50 raw on `vmi1153651`: `wcsrchr_absent_16=12.876 ns`, `64=46.986 ns`, `256=200.626 ns`, `1024=785.267 ns`, `4096=2895.479 ns`.
- Before/after p50 raw: `wcsrchr_absent_16 19.380 -> 12.876 ns`, `64 73.648 -> 46.986 ns`, `256 274.002 -> 200.626 ns`, `1024 1052.235 -> 785.267 ns`, `4096 4464.526 -> 2895.479 ns`.
- Existing `wcsrchr` test-line sha256 matched baseline: `2575c670f2a1afd3210dfbad1abcaf8ea96386a31a5693328afa90eefde43025`; full post hash including the new regression is `310fdc0bccac74f6e2a12ba0d659608176e5715caec577bd54b6365f164028a2`.
- Validation: `cargo test -p frankenlibc-core wcsrchr -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core` and `git diff --check` passed locally.
- Source sha256: `b4cb14cd2dd5a2b02313c0fbcaca34bcb29607b55f28e1caac78575809cea9b4`.

---

Objective: apply extreme-software-optimization to `bd-iskiih` after the ready `[perf]` queue was drained.

Pass 1 - Fresh profile-backed target selection: complete.
- Started: 2026-06-02T13:25:06-04:00.
- Target: residual `wcsrchr` non-NUL absent scans in `crates/frankenlibc-core/src/string/wide.rs`.
- Evidence: fresh rch baseline job `29869223945702214` on `vmi1156319` measured `wcsrchr_absent_4096=2656.009 ns` p50 after the previous `bd-jplosq` four-lane unroll, with `1024=695.376 ns`.
- Candidate lever: inspect the existing safe wide scan for one additional loop-shape change that preserves NUL-bounded last-match semantics.

Pass 2 - Baseline and behavior proof setup: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench wcsrchr_absent -- --sample-size 50 --noplot`.
- Baseline p50 raw on `vmi1156319`: `wcsrchr_absent_16=11.858 ns`, `64=44.377 ns`, `256=171.729 ns`, `1024=695.376 ns`, `4096=2656.009 ns`.
- Baseline p95 raw on `vmi1156319`: `wcsrchr_absent_16=21.250 ns`, `64=72.750 ns`, `256=234.719 ns`, `1024=866.829 ns`, `4096=3242.828 ns`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: safe-Rust block skip for clean wide-character chunks, then exact left-to-right candidate resolution.
- Recommendation card and fallback trigger recorded in `tests/artifacts/perf/bd-iskiih-wcsrchr-eight-wide-skip.md`.
- Pre-edit direct rch behavior proof passed 5/5 existing `wcsrchr` tests on `vmi1153651` via job `29869223945702232`.
- Existing `wcsrchr` test-line sha256: `310fdc0bccac74f6e2a12ba0d659608176e5715caec577bd54b6365f164028a2`.
- Isomorphism: NUL ordering, last-match tie-breaking, unterminated-slice behavior, and the `c == 0` branch are preserved; floating-point and RNG are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: changed only the `c != 0` scan from four-lane mandatory resolution to eight-lane clean-block skipping with exact candidate resolution.
- Added `test_wcsrchr_skipped_chunks_resolve_candidate_order` to pin skip-over-clean-chunk behavior, last-match choice, and after-NUL ignoring.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench wcsrchr_absent -- --sample-size 50 --noplot`.
- Post p50 raw on `vmi1149989`: `wcsrchr_absent_16=8.122 ns`, `64=19.535 ns`, `256=71.502 ns`, `1024=381.399 ns`, `4096=1213.134 ns`.
- Before/after p50 raw: `wcsrchr_absent_16 11.858 -> 8.122 ns`, `64 44.377 -> 19.535 ns`, `256 171.729 -> 71.502 ns`, `1024 695.376 -> 381.399 ns`, `4096 2656.009 -> 1213.134 ns`.
- Existing five-line `wcsrchr` test sha256 stayed `310fdc0bccac74f6e2a12ba0d659608176e5715caec577bd54b6365f164028a2`; full six-line post hash is `fd2bde19273a0dea5b7c0b6526135f3470f4eeebc5764f5b259eb9a34043e258`.
- Validation: `cargo test -p frankenlibc-core wcsrchr -- --nocapture` and `cargo check -p frankenlibc-core --all-targets` passed via rch; `cargo fmt -p frankenlibc-core --check` and `git diff --check` passed locally.
- Clippy note: `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` was run via rch and failed in unrelated dirty `crates/frankenlibc-core/src/string/mem.rs` (`clippy::manual_find` at line 173), outside this bead's touched/staged surface.
- Score: Impact 4 x Confidence 5 / Effort 1 = 20.0.

---

Completion update for `bd-usvgpi`: stdio line-buffered trailing-newline fast path.

Pass 1 - Profile triage and target scoring: complete.
- Fresh RCH `stdio_bench` baseline on `vmi1293453`: `stdio_stream_buffer/line_buffered_write=405.94 ns` p50, `full_buffered_write=298.37 ns` p50, so line-buffered write cost was `1.36x` the full-buffered row.
- Target: `StreamBuffer::write_line` in `crates/frankenlibc-core/src/stdio/buffer.rs`.
- Score before implementation: Impact 3 x Confidence 5 / Effort 1 = 15.0.

Pass 2 - Baseline and golden behavior: complete.
- Pre-edit deterministic RCH proof: `cargo test -p frankenlibc-core --lib stdio::buffer::tests:: -- --nocapture --test-threads=1` on `vmi1167313`, 14/14 passed.
- Existing buffer test-line sha256: `951c15bfd98af9207827166bb7ea37d5851052e91d85e4f9495c5a3b373b17ab`.

Pass 3 - Isomorphism proof and alien recommendation card: complete.
- Guard: `write_len == 0` and `data.last() == Some(b'\n')`.
- Old path would set `flush_end=data.len()`, `remainder=[]`, `flushed_from_buffer=0`, `buffered=0`, `flush_needed=true`, `flush_data=data`; the fast path returns the same observable fields and leaves the pending buffer empty.
- Ordering and tie-breaking are preserved because the only skipped scan case has the final byte as the last newline; floating-point and RNG are unaffected.
- Alien graveyard primitive: narrow hot-path specialization with constants-risk fallback. EV card: Impact 3 x Confidence 5 x Reuse 2 / Effort 1 = 30.0; fallback trigger was any behavior hash mismatch or line/full normalized regression.

Pass 4 - One-lever implementation: complete.
- Lever: added the guarded empty-pending trailing-newline fast path in `write_line`, plus `test_line_buffer_trailing_newline_empty_pending_flushes_directly`.

Pass 5 - Re-benchmark and validation: complete.
- Post deterministic RCH proof on `vmi1149989`: 15/15 buffer tests passed; existing buffer test-line sha256 stayed `951c15bfd98af9207827166bb7ea37d5851052e91d85e4f9495c5a3b373b17ab`; full post hash including the new regression is `c655eb5e70410c44e81c7ffe0bb57b0f7fdce1a1cc05334cfb20d030dfcacbc8`.
- Validation: `cargo check -p frankenlibc-core --all-targets` passed via RCH on `vmi1153651`; `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH on `vmi1167313`; `cargo fmt --check -p frankenlibc-core` and `git diff --check` passed locally.
- Post RCH `stdio_bench` on `vmi1156319`: `line_buffered_write=627.86 ns` p50, `full_buffered_write=768.00 ns` p50, so line-buffered write cost was `0.82x` the full-buffered row. Absolute before/after used different workers; normalized target ratio improved from `1.36x` to `0.82x`.
- Source sha256: `1ce944b222714e6560087dff90710fd5ac3d26105f6d8f634e429d84b0fb64dc`.
- Final score: Impact 3 x Confidence 4 / Effort 1 = 12.0, kept.

---

Completion update for `bd-perf-memchr-simd-avx2-t4ofyv`: safe portable-SIMD pre-scan for byte `memchr`/`memrchr`.

Pass 1 - Profile-backed target selection: complete.
- Target: residual large-buffer absent-scan gap in `crates/frankenlibc-core/src/string/mem.rs` after the SWAR pass.
- Fresh RCH `string_bench memchr_absent` baseline on `vmi1156319`: `16=4.731 ns`, `64=11.661 ns`, `256=40.703 ns`, `1024=147.156 ns`, `4096=546.118 ns` p50.
- The stale bead estimate was already improved by the earlier SWAR pass, but the profile still showed a vector-width gap versus glibc-style 32-byte scans on large buffers.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- memchr_absent --sample-size 50 --noplot`.
- Pre-edit deterministic behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core 'string::mem::tests::' --lib -- --nocapture --test-threads=1`.
- Post-edit deterministic behavior proof passed 38/38 `string::mem::tests::` tests on `vmi1149989`.
- Golden output sha256 over mem test lines: `8a096c5f435c70854c27e09e43d7da789aa267862d35be905a66cc8667561b2c`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: safe portable-SIMD packed equality probe over 32-byte panels, followed by exact scalar resolution only for panels whose mask reports a candidate.
- Fallback trigger: revert the SIMD panel probe if the deterministic test-line hash changes, if any first/last match index changes, or if the rebench regresses the 1024/4096 p50 targets.
- Isomorphism: `n` clamping is unchanged; absent panels only skip work when every byte is proven unequal; matching panels still resolve first-match order with `position` and last-match order with `rposition`; SWAR and scalar tails are retained unchanged for remainders. Floating-point, RNG, and error classes are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: added `std::simd::Simd<u8, 32>` equality probes ahead of the existing 8-byte SWAR loops in `memchr` and `memrchr`.
- Added tests `test_memchr_simd_chunk_resolves_first_match` and `test_memrchr_simd_chunk_resolves_last_match` to pin cross-panel ordering.
- Source sha256: `93463834cec88257256364b19c30713de0a055eac4b173ceee4c18ad25926eed` for `src/lib.rs`; `9c2be2250fb0e3f8dcbca340e2ae0b443b6406cf657a2e995862ed442d4a84e8` for `src/string/mem.rs`.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- memchr_absent --sample-size 50 --noplot`.
- Post p50 raw on `vmi1149989`: `16=3.395 ns`, `64=7.371 ns`, `256=25.555 ns`, `1024=99.313 ns`, `4096=398.166 ns`.
- Before/after p50 raw: `memchr_absent_16 4.731 -> 3.395 ns`, `64 11.661 -> 7.371 ns`, `256 40.703 -> 25.555 ns`, `1024 147.156 -> 99.313 ns`, `4096 546.118 -> 398.166 ns`.
- Validation: `cargo test -p frankenlibc-core 'string::mem::tests::' --lib -- --nocapture --test-threads=1` passed via RCH; `cargo check -p frankenlibc-core --all-targets` passed via RCH; `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` and `git diff --check` passed locally.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0, kept.

---

Completion update for `bd-2g7oyh.1`: safe portable-SIMD `find_byte_or_nul` pre-scan for `strchr`/`strchrnul`.

Pass 1 - Profile-backed target selection: complete.
- Parent directive: `bd-2g7oyh` no-gaps safe-Rust string/memory scan parity.
- Fresh RCH filtered `string_bench` profile on `vmi1227854`: `strchr_absent_4096=537.672 ns`, `strchrnul_absent_4096=521.268 ns`, with `strchr_absent_1024=130.152 ns` and `strchrnul_absent_1024=118.900 ns` p50.
- Target: `find_byte_or_nul` in `crates/frankenlibc-core/src/string/str.rs`, shared by `strchr`, `strchrnul`, single-reject `strcspn`, single-accept `strpbrk`, and `strsep`.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- 'strlen|strchr_absent|strchrnul_absent|strrchr_absent|memcmp|strcmp' --sample-size 50 --noplot`.
- Pre-edit deterministic behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core 'string::str::tests::' --lib -- --nocapture --test-threads=1`.
- Pre-edit behavior proof passed 100/100 `string::str::tests::` tests on `vmi1227854`.
- Existing test-line sha256: `7d2683a7daf15fdf693e9ff3fd3d9789343feb8d8a47992b66479e6dbae6f5aa`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: vectorized execution over contiguous scan panels with constants-risk fallback.
- Recommendation card: Impact 3 x Confidence 4 / Effort 1 = 12.0; fallback trigger was any behavior hash mismatch, first-match/NUL ordering change, or 1024/4096 p50 regression.
- Isomorphism: the SIMD probe only skips a 32-byte panel if every byte is proven neither `needle` nor NUL; candidate panels are resolved by the original left-to-right scalar byte check, preserving first occurrence and NUL-before-needle ordering. The `needle == 0` case returns the first NUL as before. Floating-point, RNG, and error classes are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: added `std::simd::Simd<u8, 32>` equality probes for `needle` and NUL before the existing SWAR/scalar tail in `find_byte_or_nul`.
- Added `test_strchr_simd_panel_resolves_needle_before_later_nul` and `test_strchr_simd_panel_stops_at_nul_before_later_needle`.
- Source sha256: `1091eae4f61707c2a495b6f398d28d1b72fe763c3873a267b32a3d325edf5353`.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- 'strchr_absent|strchrnul_absent' --sample-size 50 --noplot`.
- Post p50 raw on `vmi1227854`: `strchr_absent_16=6.291 ns`, `64=7.179 ns`, `256=12.726 ns`, `1024=37.320 ns`, `4096=141.642 ns`; `strchrnul_absent_16=5.445 ns`, `64=6.511 ns`, `256=12.922 ns`, `1024=39.010 ns`, `4096=137.769 ns`.
- Before/after p50 raw: `strchr_absent_1024 130.152 -> 37.320 ns`, `4096 537.672 -> 141.642 ns`; `strchrnul_absent_1024 118.900 -> 39.010 ns`, `4096 521.268 -> 137.769 ns`.
- Existing test-line sha256 stayed `7d2683a7daf15fdf693e9ff3fd3d9789343feb8d8a47992b66479e6dbae6f5aa`; full post hash including the two new tests is `81785b5cab1813d55cce7940f69ce34b4648c883d26b23566b4c9931fddb8b91`.
- Validation: `cargo test -p frankenlibc-core 'string::str::tests::' --lib -- --nocapture --test-threads=1` passed via RCH; `cargo check -p frankenlibc-core --all-targets` passed via RCH; `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` and `git diff --check` passed locally.
- Score: Impact 4 x Confidence 4 / Effort 1 = 16.0, kept.

---

Completion update for `bd-2g7oyh.2`: adaptive sparse probing for direct malloc/free elimination handoff.

Pass 1 - Profile-backed target selection: complete.
- Parent directive: `bd-2g7oyh` no-gaps safe-Rust allocator parity.
- Fresh RCH `glibc_baseline_malloc_free_64` baseline on `vmi1156319`: `frankenlibc_core_state` p50 `4131.462 ns/op`, p95 `4556.850`, p99 `6159.813`; `host_glibc` p50 `8.091 ns/op`, p95 `13.750`, p99 `45.064`.
- Target: direct `MallocState` 64B malloc/free cycle, with a ~510.6x p50 gap vs host glibc.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env CARGO_TARGET_DIR=/data/tmp/rch_target_frankenlibc_malloc_profile_baseline cargo bench -p frankenlibc-bench --bench glibc_baseline_bench -- glibc_baseline_malloc_free_64 --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- Pre-edit deterministic behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core malloc -- --nocapture`.
- Pre-edit behavior proof passed 54 malloc unit tests plus 1 allocator property test on `vmi1149989`.
- Normalized malloc test transcript sha256: `6d2a95f4e32d2a92e56ebcc3bb9e1e130e645ea61c1672380fc6c130866bffdc`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: adaptive sparse probing for contention structures; treat direct handoff as sampled under low-success evidence instead of mandatory on every one-sided free.
- Recommendation card: Impact 5 x Confidence 4 / Effort 1 = 20.0; fallback trigger was any golden hash mismatch, failed symmetric/direct-exchange tests, or post-rch p50 regression.
- Isomorphism: allocation/free return values, accounting, error classes, and no-partner lifecycle fallback path stay unchanged; successful direct exchange remains covered by existing cross-thread and symmetric workload tests. Floating-point, RNG, and tie-breaking are unaffected. `EliminationStats.disabled_remaining` changes intentionally as controller telemetry.

Pass 4 - One-lever implementation: complete.
- Lever: changed `ADAPTIVE_WINDOW` from `1000` to `64` and `ADAPTIVE_DISABLE_WINDOW` from `1000` to `4096` in `crates/frankenlibc-core/src/malloc/elimination.rs`.
- Source sha256: `00d25c49f87b9eac13126fb6639920b040c64f6fd2428a8b1ab2615d0ac09745`.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env CARGO_TARGET_DIR=/data/tmp/rch_target_frankenlibc_malloc_profile_post cargo bench -p frankenlibc-bench --bench glibc_baseline_bench -- glibc_baseline_malloc_free_64 --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- Post p50 raw on `vmi1149989`: `frankenlibc_core_state=488.928 ns/op`, `host_glibc=3.864 ns/op`.
- Before/after p50 raw: `frankenlibc_core_state malloc_free_64 4131.462 -> 488.928 ns/op` (8.45x speedup, 88.2% p50 reduction).
- Normalized malloc test transcript sha256 stayed `6d2a95f4e32d2a92e56ebcc3bb9e1e130e645ea61c1672380fc6c130866bffdc`.
- Validation: `cargo test -p frankenlibc-core malloc -- --nocapture` passed via RCH; `cargo check -p frankenlibc-core --all-targets` passed via RCH; `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` passed locally with `TMPDIR=/data/tmp`. Workspace `cargo fmt --check` still fails on unrelated harness test formatting outside this reserved/touched surface.
- Score: Impact 5 x Confidence 4 / Effort 1 = 20.0, kept.

---

Completion update for `bd-2g7oyh.3`: safe portable-SIMD reverse panels for `strrchr`.

Pass 1 - Profile-backed target selection: complete.
- Parent directive: `bd-2g7oyh` no-gaps safe-Rust string/memory scan parity.
- Fresh RCH `strrchr_absent` baseline on `vmi1156319`: p50 raw `16=11.657 ns`, `64=21.310 ns`, `256=64.062 ns`, `1024=245.286 ns`, `4096=935.374 ns`.
- Target: `find_last_byte_before` in `crates/frankenlibc-core/src/string/str.rs`; the reverse scan still used 8-byte SWAR chunks after `strlen`.

Pass 2 - Baseline and golden behavior: complete.
- Pre-edit deterministic behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core 'string::str::tests::' --lib -- --nocapture --test-threads=1`.
- Pre-edit behavior proof passed 102/102 `string::str::tests::` tests on `vmi1156319`.
- Existing test-line sha256: `2525355f145b2beeb284d482bdd4928750f0094baf9c319f473daed8ae5ae7dc`.
- Pre-edit source sha256: `1091eae4f61707c2a495b6f398d28d1b72fe763c3873a267b32a3d325edf5353`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: SIMD group probing over contiguous byte panels, analogous to Swiss-table 16/32-byte metadata probes but applied directly to the searched byte string.
- Recommendation card: EV `(Impact 3 * Confidence 4 * Reuse 2) / (Effort 1 * Friction 1) = 24.0`; fallback trigger was any existing-test hash mismatch, changed last-match/terminator behavior, or post-rch p50 regression at 1024/4096.
- Isomorphism: `strrchr(c != 0)` still bounds the reverse scan with `strlen(s)` first; the SIMD probe only skips a 32-byte panel if every byte is proven unequal to `needle`; candidate panels resolve with the same scalar right-to-left search, preserving last-match tie-breaking and ignoring bytes after the terminator. Floating-point, RNG, and error classes are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: added `std::simd::Simd<u8, 32>` equality probes to `find_last_byte_before`, scanning 32-byte panels from right to left before the existing SWAR/scalar tail.
- Added `test_strrchr_simd_panel_resolves_last_match_before_terminator` and `test_strrchr_simd_panel_ignores_match_after_terminator`.
- Source sha256: `1e003b69f927c7719ffe0e90158c941076e287ef023f60c3d226e0ae902890be`.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- strrchr_absent --sample-size 50 --noplot`.
- Post p50 raw on `vmi1149989`: `16=8.606 ns`, `64=13.398 ns`, `256=32.036 ns`, `1024=145.570 ns`, `4096=390.288 ns`.
- Before/after p50 raw: `strrchr_absent_16 11.657 -> 8.606 ns`, `64 21.310 -> 13.398 ns`, `256 64.062 -> 32.036 ns`, `1024 245.286 -> 145.570 ns`, `4096 935.374 -> 390.288 ns`.
- Existing test-line sha256 stayed `2525355f145b2beeb284d482bdd4928750f0094baf9c319f473daed8ae5ae7dc`; full post hash including the two new tests is `235e4da93b185ebe3f01964dd290d33f97b492e083f8bd5e4fbc39208023a507`.
- Validation: `cargo test -p frankenlibc-core 'string::str::tests::' --lib -- --nocapture --test-threads=1` passed via RCH; `cargo check -p frankenlibc-core --all-targets` passed via RCH; `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` and `git diff --check` passed locally.
- Score: Impact 4 x Confidence 4 / Effort 1 = 16.0, kept.

---

Completion update for `bd-wkcldb`: qsort small-partition insertion cutoff.

Pass 1 - Profile-backed target selection: complete.
- Fresh RCH `glibc_baseline_qsort_128_i32` baseline on `vmi1156319`: `frankenlibc_core` p50 `5380.495 ns/op`, p95 `6147.209`, p99 `6313.427`; `host_glibc` p50 `4150.123 ns/op`, p95 `5123.539`, p99 `5170.096`.
- Target: `crates/frankenlibc-core/src/stdlib/sort.rs` `quicksort_safe`, where tiny partitions were still paying recursive partition and median-three overhead until only the depth-limit fallback switched to insertion sort.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core 'stdlib::sort' --lib -- --nocapture --test-threads=1`.
- Pre-edit behavior proof passed 24/24 `stdlib::sort` tests on `vmi1153651`.
- Existing test-line sha256: `79813919c2285067a39ded724b4bfb207599fb72692c708fa69ee716ef699ab1`.
- Pre-edit source sha256: `bb37c46c840cf3469f6662d583f8d4e097cc2961b66f49dfe75016d97eac0283`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: cache-local hybrid quicksort/insertion sort for tiny partitions where constants dominate.
- Recommendation card: EV `(Impact 3 * Confidence 4 * Reuse 2) / (Effort 1 * Friction 1) = 24.0`; fallback trigger was any existing-test hash mismatch, sorted permutation regression, or post-rch p50 regression.
- Isomorphism: `qsort` promises sorted output by comparator but not stability or comparator-call ordering; the cutoff only changes the algorithm for partitions of 16 elements or fewer, preserving sorted multiset output. Floating-point, RNG, errno, and ABI return behavior are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: added `QSORT_INSERTION_CUTOFF = 16` and direct `insertion_sort` dispatch for `count <= 16`.
- Added `qsort_small_partition_cutoff_preserves_sorted_multiset`.
- Source sha256: `f54102e160c31e4c53f9cf18baeb08284f2a0dc26eebe6cb7d42e399c85537af`.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench glibc_baseline_bench -- glibc_baseline_qsort_128_i32 --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- Post p50 raw on `vmi1149989`: `frankenlibc_core=2878.460 ns/op`, `host_glibc=2329.633 ns/op`.
- Before/after p50 raw: `frankenlibc_core qsort_128_i32 5380.495 -> 2878.460 ns/op` (1.87x speedup, 46.5% p50 reduction).
- Existing test-line sha256 stayed `79813919c2285067a39ded724b4bfb207599fb72692c708fa69ee716ef699ab1`; full post hash including the new test is `54cedf87b745df0c0948299a8de213593c5941cd27ed8c23cd134b7c7116c130`.
- Validation: `cargo test -p frankenlibc-core 'stdlib::sort' --lib -- --nocapture --test-threads=1` passed via RCH; `cargo check -p frankenlibc-core --all-targets` passed via RCH; `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` and `git diff --check` passed locally.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0, kept.

---

Completion update for `bd-2g7oyh.6`: safe portable-SIMD folded first-byte probe for `strcasestr`.

Pass 1 - Fresh profile-backed target selection: complete.
- Ready child perf beads were drained, so a fresh RCH string profile selected residual substring-search gaps.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- 'strstr_absent|strnstr_bounded_absent|strcasestr_absent|wcsstr_absent' --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Baseline p50 raw on `vmi1153651`: `strcasestr_absent_4096=6288.487 ns`, `strstr_absent_4096=4720.812 ns`, `wcsstr_absent_4096=3044.435 ns`, `strnstr_bounded_absent_4096=2889.390 ns`.
- Target: `strcasestr` byte-at-a-time absent first-needle scan in `crates/frankenlibc-core/src/string/str.rs`.

Pass 2 - Baseline and golden behavior: complete.
- Pre-edit behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core strcasestr -- --nocapture --test-threads=1`.
- Pre-edit 7/7 existing `strcasestr` tests passed.
- Existing test-line sha256: `1c2254be74fa7e1a7cf6dd68001290fe83e8f2097a703967c48300fcc33c2b1c`.
- Pre-edit source sha256: `b8916a39c9447328dbd20cbef14d7054362572683f2bba13c5ffc75fac2eac81`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: Swiss-table-style SIMD group probing over 32 packed bytes, adapted from metadata probes to first-byte candidate discovery.
- Recommendation card: Impact 4 x Confidence 5 / Effort 2 = 10.0; fallback trigger was any existing-test hash mismatch, NUL/candidate ordering regression, or post-RCH p50 target regression.
- Subagent review confirmed the hard condition: NUL must win over any later folded first-byte hit in the same panel.
- Isomorphism: candidate discovery only skips panels where every byte is proven neither NUL nor ASCII-case-equal to the first needle byte; full scalar needle verification, first-match tie-breaking, unterminated-slice behavior, ASCII-only folding, and non-ASCII exactness are preserved. Floating-point and RNG are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: added a private safe `Simd<u8, 32>` folded first-byte-or-NUL probe and routed `strcasestr` candidate discovery through it.
- Added tests for NUL-before-candidate ordering, folded candidate before NUL, false first-byte candidate before later true match, and non-ASCII first-byte exactness.
- Post source sha256: `fa9ca8c3716de18535ece65fde46ad9bc262672dfb8b123a960db547a4ba9003`.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- strcasestr_absent --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Post p50 raw on same worker `vmi1153651`: `16=30.425 ns`, `64=26.544 ns`, `256=66.875 ns`, `1024=217.227 ns`, `4096=845.828 ns`.
- Before/after p50 raw: `strcasestr_absent_64 95.585 -> 26.544 ns`, `256 375.066 -> 66.875 ns`, `1024 1404.287 -> 217.227 ns`, `4096 6288.487 -> 845.828 ns`; 16B was `29.623 -> 30.425 ns`, within small-benchmark noise.
- Existing seven-line `strcasestr` test sha256 stayed `1c2254be74fa7e1a7cf6dd68001290fe83e8f2097a703967c48300fcc33c2b1c`; full eleven-line post hash is `187c904c52d44834a3cef6f514c4455c096681323cd5a3d8ed3431e11340d877`.
- Validation: `cargo test -p frankenlibc-core strcasestr -- --nocapture --test-threads=1`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` and `git diff --check` passed locally.
- Score: Impact 4 x Confidence 5 / Effort 2 = 10.0, kept.

---

Completion update for `bd-2g7oyh.8`: four-byte general string-set SIMD membership probe.

Pass 1 - Fresh profile-backed target selection: complete.
- Ready child perf beads were drained after `wcsstr`, so a fresh RCH memory/string scan profile selected residual four-byte general set scans.
- Baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- 'memcmp|memchr_absent|strchr_absent|strchrnul_absent|strcspn_absent|strcspn_general_absent|strpbrk_absent|strpbrk_general_absent|strspn_full|strspn_general_full' --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Baseline p50 raw on `vmi1153651`: `strspn_general_full_4096=4504.150 ns`, `strpbrk_general_absent_4096=3242.898 ns`, `strcspn_general_absent_4096=3097.405 ns`, `memchr_absent_4096=808.281 ns`, `memcmp_4096=317.025 ns`.
- Target: `crates/frankenlibc-core/src/string/str.rs` general four-byte accept/reject paths for `strspn`, `strcspn`, and `strpbrk`.

Pass 2 - Baseline and golden behavior: complete.
- Pre-edit behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core 'string::str::tests::test_str' --lib -- --nocapture --test-threads=1`.
- Pre-edit 93/93 string tests passed.
- Existing string-set test sha256: `52f00e2c350db953e6def9fbc6fbc946bad7682750b6606a7fc83240f900bc55`.
- Pre-edit source sha256: `fa9ca8c3716de18535ece65fde46ad9bc262672dfb8b123a960db547a4ba9003`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: Swiss-table-style SIMD group probing over packed control bytes, adapted to direct string bytes and four candidate bytes.
- Recommendation card: Impact 4 x Confidence 5 / Effort 2 = 10.0; fallback trigger was any existing-test hash mismatch, NUL/first-match ordering regression, or focused post-RCH p50 regression.
- Isomorphism: candidate discovery only skips 32-byte panels where every lane is proven irrelevant to the original stop condition; scalar left-to-right resolution inside candidate panels preserves first stop/match, tie-breaking, exact byte equality, and NUL ordering. Floating-point and RNG are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: added private safe `Simd<u8, 32>` any-of-four-or-NUL and not-any-of-four-or-NUL panel predicates, then routed only four-byte accept/reject sets through them.
- Added six focused tests covering first-match, first-nonmember, and NUL-before-candidate ordering for `strspn`, `strcspn`, and `strpbrk`.
- Post source sha256: `db712179b0cbbed1e260388d2cb96c5779d411a57bd0d21a4a9e4a1e1d710725`.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench -- 'strspn_general_full|strpbrk_general_absent|strcspn_general_absent' --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Post p50 raw on same worker `vmi1153651`: `strcspn_general_absent_4096=439.767 ns`, `strpbrk_general_absent_4096=432.371 ns`, `strspn_general_full_4096=458.503 ns`.
- Before/after p50 raw: `strspn_general_full_4096 4504.150 -> 458.503 ns`, `strpbrk_general_absent_4096 3242.898 -> 432.371 ns`, `strcspn_general_absent_4096 3097.405 -> 439.767 ns`; the 16B `strpbrk` case regressed `28.150 -> 40.499 ns`, but the profiled long-scan target and all 64B+ cases clear the score gate.
- Existing string-set test sha256 stayed `52f00e2c350db953e6def9fbc6fbc946bad7682750b6606a7fc83240f900bc55`; full post string-set no-aggregate hash is `a4010e3ce10d786e13b630ab717a262906c4470bdf5fd03c5e9284d2adf29012`.
- Validation: `cargo test -p frankenlibc-core 'string::str::tests::test_str' --lib -- --nocapture --test-threads=1`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` and reserved-file `git diff --check` passed locally.
- Score: Impact 4 x Confidence 5 / Effort 2 = 10.0, kept.

---

Completion update for `bd-2g7oyh.9`: netgroup parser first-token early reject.

Pass 1 - Fresh profile-backed target selection: complete.
- Ready child perf beads were drained, so fresh non-string RCH profiles compared `stdio_bench` and `resolv_parsers_bench`.
- `stdio_bench` top rows were stream setup/buffering heavy: `full_buffered_write=717.36 ns`, `line_buffered_write=690.41 ns`.
- Parser baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench resolv_parsers_bench`.
- Parser baseline p50 raw on `vmi1153651`: `parse_netgroup_triples_match=775.335 ns`, while sibling parser rows were `154.435..196.953 ns`.
- Target: `crates/frankenlibc-core/src/netgroup/mod.rs::parse_netgroup_triples`.

Pass 2 - Baseline and golden behavior: complete.
- Pre-edit behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core netgroup --lib -- --nocapture --test-threads=1`.
- Pre-edit 15/15 netgroup tests passed.
- Existing netgroup test-line sha256: `379ccb0b102eb727a6f7fa2568c6c4727acc492c5018ff21a89a3554a84c6a9d`.
- Pre-edit source sha256: `bc39ed92ccce19d695ff81d90c954d1ca9dc059b682bffd84497caad126b9f6c`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: ART-style prefix-gated lookup/path compression from section 7.5, plus staged early-exit validation pattern.
- Recommendation card: Impact 3 x Confidence 4 / Effort 1 = 12.0; fallback trigger was any existing-test hash mismatch, comment/first-token semantic regression, or post-RCH p50 regression.
- Isomorphism: line order, duplicate-group accumulation, first-token tie-breaking, case-insensitive group matching, comment truncation, triple ordering, field trimming, and missing-field defaults are preserved. Floating-point and RNG are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: replaced the per-line split/filter prelude with `first_token_bounds`, so blank, comment, and nonmatching lines are rejected before comment stripping and triple extraction.
- Added tests for inline-comment-hidden triples and `group#comment` tails.
- Post source sha256: `20b75904117ab5ea83dbc29836aa4ea02efa60b219be6d077b48bf5207a28621`.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench resolv_parsers_bench`.
- Post p50 raw on same worker `vmi1153651`: `parse_netgroup_triples_match=601.990 ns`; p95 `819.517 ns`; p99 `949.700 ns`.
- Before/after p50 raw: `parse_netgroup_triples_match 775.335 -> 601.990 ns`; p95 `1425.271 -> 819.517 ns`; p99 `1827.021 -> 949.700 ns`.
- Existing netgroup test sha256 stayed `379ccb0b102eb727a6f7fa2568c6c4727acc492c5018ff21a89a3554a84c6a9d`; full post 17-test hash is `2d3d6fbe5d75d95adbd61791ea095821c64e99944eaf7dff9d276e4455125ff0`.
- Validation: `cargo test -p frankenlibc-core netgroup --lib -- --nocapture --test-threads=1`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` and reserved-file `git diff --check` passed locally.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0, kept.

---

Completion update for `bd-2g7oyh.10`: malloc lifecycle drain capacity retention.

Pass 1 - Fresh profile-backed target selection: complete.
- Ready child perf beads were drained, so the latest RCH glibc-baseline profile selected the allocator residual.
- Campaign profile worker `vmi1227854`: `malloc_free_64` FrankenLibC p50 `762.470 ns/op`, p95 `1462.652`, p99 `1903.000`; host glibc p50 `5.000`, p95 `12.500`, p99 `80.000`.
- Focused pre-edit command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench glibc_baseline_malloc_free_64 -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Focused pre-edit worker `vmi1153651`: FrankenLibC p50 `1346.311 ns/op`, p95 `1713.000`, p99 `1997.703`; host glibc p50 `8.494`, p95 `11.875`, p99 `40.500`.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::drain_lifecycle_logs`, because the benchmark drains lifecycle logs after `len() > 2048`.

Pass 2 - Baseline and golden behavior: complete.
- Pre-edit behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core malloc::allocator::tests:: -- --nocapture`.
- Pre-edit 10/10 allocator tests passed.
- Existing allocator test-line sha256: `7f23ca2d04850e50e6f1c24919633e0d02d1dbfcfdd07df72eb43baed086c6ba`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: hot/cold path separation plus capacity-retaining evidence-log buffers, preserving evidence records while avoiding repeated buffer regrowth after drains.
- Recommendation card: Impact 2 x Confidence 5 / Effort 1 = 10.0; fallback trigger was any existing-test hash mismatch or post-RCH benchmark failure.
- Isomorphism: lifecycle record order, decision IDs, trace IDs, fields, malloc/free pointer reuse order, thread-cache LIFO order, counters, and empty-after-drain postcondition are preserved. Floating-point and RNG are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: `drain_lifecycle_logs` now uses `self.lifecycle_logs.drain(..).collect()` so returned records are unchanged and the internal log buffer capacity is retained.
- Added a focused allocator regression proving drained records are byte-for-byte identical, `lifecycle_logs()` is empty after drain, capacity is retained, and subsequent lifecycle IDs continue monotonically.
- Post source sha256: `07ef3731cb393f62293f714cb65d9944275939d80f7af00d28597b29b82a45a1`.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench glibc_baseline_malloc_free_64 -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Post worker `vmi1227854`: FrankenLibC p50 `687.692 ns/op`, p95 `743.009`, p99 `832.652`; host glibc p50 `4.506`, p95 `10.281`, p99 `40.000`.
- Before/after vs campaign same-worker profile: FrankenLibC p50 `762.470 -> 687.692 ns/op`; p95 `1462.652 -> 743.009`; p99 `1903.000 -> 832.652`.
- Existing allocator test-line sha256 stayed `7f23ca2d04850e50e6f1c24919633e0d02d1dbfcfdd07df72eb43baed086c6ba`; full post allocator hash including the new regression is `84e19d13519813915f6f97e61d4920dbd766d656bfb3f532ac0e694d22365ce0`.
- Validation: `cargo test -p frankenlibc-core malloc::allocator::tests:: -- --nocapture`, `cargo test -p frankenlibc-core malloc:: -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core` and reserved-file `git diff --check` passed locally.
- Score: Impact 2 x Confidence 5 / Effort 1 = 10.0, kept.

---

Completion update for `bd-2g7oyh.13`: strlen 64-byte safe-SIMD NUL panels.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/string/str.rs::strlen`.
- Baseline from RCH host-comparison reprofile on worker `vmi1153651`: `strlen_4096` FrankenLibC p50 `162.620 ns/op`, p95 `210.000`, p99 `257.515`; host glibc p50 `36.983`, p95 `57.331`, p99 `231.000`.
- Candidate lever: widen the existing candidate-only safe-SIMD NUL panel from 32 bytes to 64 bytes, then keep the exact existing word/scalar resolver for first-NUL ordering.
- Score: Impact 4 x Confidence 4 x Reuse 2 / Effort 1 = 32.0.

Pass 2 - Golden behavior: complete.
- Fixture sha256 verified with `sha256sum -c -`: `tests/conformance/fixtures/string_ops.json=27cc53f44e4d83352210d2e7b305cfff2729276ce31e31b03e24116f831b2f89`, `tests/conformance/fixtures/strlen_strict.json=b5509edb2fc90403daf10fbef4944369aff58e26569d6a03b77b6317c646667f`.
- RCH behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core strlen -- --nocapture`.
- RCH behavior result on `vmi1153651`: 6 focused `strlen` unit tests plus `string_properties::prop_strlen_finds_first_nul` passed.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: contiguous SIMD group probing / Swiss-table-style control-byte panels, applied to the string bytes as the control plane.
- Ordering is preserved because SIMD only detects whether a 64-byte panel may contain NUL; exact first-NUL index is still resolved by the existing aligned-word and scalar left-to-right scan.
- Tie-breaking is unchanged: `strlen` has no competing candidate other than the first NUL; callers such as `strrchr(s, 0)` still consume the same returned length.
- Floating-point and RNG behavior are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: introduced `STRLEN_SIMD_LANES = 64` and `has_nul_simd_64`, then advanced the `strlen` candidate scan in 64-byte panels.
- Added `test_strlen_wide_simd_panel_finds_nul_after_first_panel` to pin first-NUL behavior when a later NUL exists in the same slice.
- No ABI changes, no benchmark harness changes, no C linkage, and no global state.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench glibc_baseline_strlen_4096 -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Post benchmark on worker `vmi1149989`: FrankenLibC `strlen_4096` p50 `59.921 ns/op`, p95 `102.009`, p99 `105.819`; host glibc p50 `23.728`, p95 `50.518`, p99 `70.000`.
- Before/after p50 versus bead baseline: `strlen_4096 162.620 -> 59.921 ns/op`.
- Validation: `cargo test -p frankenlibc-core strlen -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core`, golden fixture `sha256sum -c`, and reserved-file `git diff --check` passed locally.
- Score: Impact 4 x Confidence 5 / Effort 1 = 20.0, kept.

---

Completion update for `bd-2g7oyh.12`: malloc lifecycle static-detail borrowing.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::record_lifecycle`.
- Baseline from bead RCH profile on worker `vmi1149989`: `malloc_free_64` FrankenLibC p50 `617.985 ns/op`, p95 `1002.000`, p99 `1031.141`; host glibc p50 `3.509`, p95 `12.500`, p99 `60.000`.
- Source evidence: hot malloc/free cycles emit three lifecycle records and previously materialized static `details` strings as owned `String`s on every record.
- Candidate lever: keep lifecycle ledger contents unchanged while storing static hot-path detail strings as borrowed `Cow<'static, str>`.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0.

Pass 2 - Golden behavior: complete.
- RCH behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core malloc::allocator::tests:: -- --nocapture`.
- RCH behavior result on `vmi1153651`: 12 focused allocator tests passed, including `hot_cycle_static_lifecycle_details_are_borrowed`.
- Full post allocator test-line sha256: `4edf391a3737092e0ec19b14f5af11fd0d574dfbe2a3b0ac435c0bb7653100ad`.
- Golden fixture sha256 recorded: `tests/conformance/allocator_membrane_deterministic_sequence_completion_contract.v1.json=6779172d3b50749a8382aeb9ec5c92dccb6e27214eb8e6d872c827b2cc8baba4`, `tests/conformance/allocator_membrane_invariant_completion_contract.v1.json=771360fa63c7e9e4ab91bcab125d34b111a438fe681599504b082461678c01bb`, `tests/conformance/perf_regression_prevention.v1.json=83afd8b175d8ed6d5f2c7c9d34ce536de1f4465b96e9e6f1aebcb8077a7451dc`, `tests/conformance/perf_budget_policy.json=8d48d9ff62ed7ff2d614ee8e62f5a59f699903da8384d331fff1b213377f2c24`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: fixed-format hot-path evidence encoding with hot/cold payload separation.
- Ordering is preserved because `record_lifecycle` still obtains the same monotonically increasing decision id before pushing one `AllocatorLogRecord`.
- Tie-breaking is unchanged: allocator reuse order, thread-cache LIFO behavior, central-bin order, and elimination outcomes are not modified.
- Lifecycle fields are preserved: `trace_id`, symbol, event, ptr, size, bin, outcome, counters, and detail text are unchanged; only the storage owner for static detail strings changes.
- Floating-point and RNG behavior are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: `AllocatorLogRecord.details` is now `Cow<'static, str>`, and `record_lifecycle` accepts `impl Into<Cow<'static, str>>`.
- Added a focused hot-cycle regression proving dynamic certificate details remain owned while static thread-cache malloc/free details are borrowed.
- No benchmark harness changes, no allocator policy changes, no C linkage, and no edits to peer-owned `malloc/elimination.rs`.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench glibc_baseline_malloc_free_64 -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Post benchmark on same worker `vmi1149989`: FrankenLibC `malloc_free_64` p50 `537.547 ns/op`, p95 `691.953`, p99 `699.614`; host glibc p50 `5.529`, p95 `10.000`, p99 `35.000`.
- Before/after p50 versus bead baseline: `malloc_free_64 617.985 -> 537.547 ns/op`; p95 `1002.000 -> 691.953`; p99 `1031.141 -> 699.614`.
- Validation: `cargo test -p frankenlibc-core malloc::allocator::tests:: -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core`, fixture `sha256sum`, and reserved-file `git diff --check` passed locally.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0, kept.

---

Completion update for `bd-2g7oyh.16`: malloc lifecycle fixed-format trace IDs.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::record_lifecycle`.
- Fresh RCH reprofile after `73ac8c23` on worker `vmi1167313`: `malloc_free_64` FrankenLibC p50 `1377.270 ns/op`, p95 `1802.541`, p99 `1933.000`; host glibc p50 `9.371`, p95 `22.500`, p99 `65.897`.
- Focused pre-edit baseline command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench glibc_baseline_malloc_free_64 -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Focused pre-edit worker `vmi1167313`: FrankenLibC p50 `1111.491 ns/op`, p95 `1423.333`, p99 `1523.000`; host glibc p50 `8.231`, p95 `14.375`, p99 `55.500`.
- Source evidence: previous allocator passes removed drain regrowth and static detail allocation; `record_lifecycle` still constructed every `trace_id` with generic `format!("core::malloc::{}::{:016x}", symbol, decision_id)`.
- Score: Impact 2 x Confidence 4 / Effort 1 = 8.0.

Pass 2 - Golden behavior: complete.
- RCH behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env RUST_TEST_THREADS=1 cargo test -p frankenlibc-core malloc::allocator::tests:: -- --nocapture`.
- RCH behavior result on `vmi1156319`: 13 focused allocator tests passed, including `lifecycle_trace_id_matches_legacy_format`.
- Full post allocator test-line sha256: `beee9dd669e6e093b1738118a507c43455e98d05a4b6d9fd633c4086d6255e23`.
- Golden fixture sha256 recorded: `tests/conformance/allocator_membrane_deterministic_sequence_completion_contract.v1.json=6779172d3b50749a8382aeb9ec5c92dccb6e27214eb8e6d872c827b2cc8baba4`, `tests/conformance/allocator_membrane_invariant_completion_contract.v1.json=771360fa63c7e9e4ab91bcab125d34b111a438fe681599504b082461678c01bb`, `tests/conformance/perf_regression_prevention.v1.json=83afd8b175d8ed6d5f2c7c9d34ce536de1f4465b96e9e6f1aebcb8077a7451dc`, `tests/conformance/perf_budget_policy.json=8d48d9ff62ed7ff2d614ee8e62f5a59f699903da8384d331fff1b213377f2c24`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: fixed-format serialization / hot-path evidence encoding.
- Ordering is preserved because `record_lifecycle` still obtains the same decision id and pushes exactly one record per event.
- Tie-breaking is unchanged: allocator path selection, reuse order, thread-cache LIFO, central-bin order, and elimination outcomes are not modified.
- Trace IDs are byte-for-byte preserved: `core::malloc::`, symbol, `::`, and 16 lowercase zero-padded hex digits match the legacy `format!` expression for representative symbols and `u64::MAX`.
- Lifecycle record fields and detail text are unchanged; floating-point and RNG behavior are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: replaced the generic trace-id `format!` call with `lifecycle_trace_id`, a fixed-capacity `String` plus manual lowercase hex appending.
- Added `lifecycle_trace_id_matches_legacy_format` to pin exact formatting for malloc/free/size_class_certificate/realloc and zero/max ids.
- No benchmark harness changes, no allocator policy changes, no C linkage, and no edits to peer-owned `malloc/elimination.rs`.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench glibc_baseline_malloc_free_64 -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Post benchmark on worker `vmi1156319`: FrankenLibC `malloc_free_64` p50 `667.237 ns/op`, p95 `820.579`, p99 `1353.000`; host glibc p50 `8.381`, p95 `16.250`, p99 `40.000`.
- Before/after p50 versus focused baseline is cross-worker: `1111.491 -> 667.237 ns/op`; p95 `1423.333 -> 820.579`; p99 `1523.000 -> 1353.000`. Host p50 remained comparable (`8.231 -> 8.381`).
- Validation: `cargo test -p frankenlibc-core malloc::allocator::tests:: -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed via RCH; `cargo fmt --check -p frankenlibc-core`, fixture `sha256sum`, and reserved-file `git diff --check` passed locally.
- Score: Impact 2 x Confidence 4 / Effort 1 = 8.0, kept.

---

Completion update for `bd-2g7oyh.22`: strcmp safe-SIMD equality/NUL panels.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/string/str.rs::strcmp` equal-string hot path.
- Fresh RCH reprofile after `5866a715` on worker `vmi1227854`: `strcmp_256_equal` FrankenLibC p50 `21.842 ns/op`, p95 `38.089`, p99 `85.000`; host glibc p50 `5.636`, p95 `7.500`, p99 `20.000`.
- Focused clean baseline command from detached `HEAD` worktree: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench strcmp_256_equal -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Focused clean baseline on worker `vmi1156319`: FrankenLibC p50 `29.416 ns/op`, p95 `39.604`, p99 `180.000`; host glibc p50 `12.722`, p95 `15.691`, p99 `40.000`.
- Source evidence: `strcmp` skipped equal strings one `usize` word at a time after alignment handling while sibling string routines already used safe portable-SIMD panels.
- Score: Impact 3 x Confidence 5 / Effort 1 = 15.0.

Pass 2 - Golden behavior: complete.
- RCH golden command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core test_strcmp_golden_transcript_sha256 -- --nocapture`.
- RCH golden result on `vmi1156319`: `test_strcmp_golden_transcript_sha256` passed.
- Golden transcript SHA256: `bf3a44bce53a40a47eb334b89238d840573096846b050b62096ace20e43ff977`.
- RCH behavior command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core strcmp -- --nocapture`.
- RCH behavior result on `vmi1149989`: 8 unit tests and 3 property tests passed, including prefix, empty-string, strcoll delegation, golden transcript, and antisymmetry properties.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: SIMD group probing over a byte control plane.
- Ordering and tie-breaking are preserved because the SIMD panel is only an all-equal/no-NUL skip; any mismatch or NUL candidate falls back to the existing exact byte/word resolution from the same offset.
- First-difference semantics are unchanged: the scalar tail still returns `(a as i32) - (b as i32)` for the first differing byte.
- First-NUL semantics are unchanged: panels containing NUL do not skip, and the existing scalar tail returns zero only when both strings hit NUL together.
- Equal-string zero result is preserved by advancing over only byte-equal, NUL-free panels.
- Floating-point and RNG behavior are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: added `equal_and_no_nul_simd_32` and a 32-byte safe `Simd<u8,32>` skip loop before the existing aligned word/scalar resolution path.
- Added a focused strcmp golden transcript covering equal 256-byte strings, late differences, hidden bytes after NUL, prefixes, and unsigned byte difference sign.
- No benchmark harness changes, no allocator edits, no C linkage, and no wide-string changes.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench strcmp_256_equal -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Post benchmark on worker `vmi1149989`: FrankenLibC `strcmp_256_equal` p50 `7.905 ns/op`, p95 `10.797`, p99 `50.500`; host glibc p50 `4.862`, p95 `7.373`, p99 `20.000`.
- Before/after p50 versus clean focused baseline: `29.416 -> 7.905 ns/op`; p95 `39.604 -> 10.797`; p99 `180.000 -> 50.500`.
- Validation: `cargo test -p frankenlibc-core strcmp -- --nocapture` passed via RCH; `cargo check -p frankenlibc-core --lib` passed via RCH; `cargo clippy -p frankenlibc-core --lib -- -D warnings` is blocked by pre-existing `string/wide.rs` warnings at lines 472/478 and `manual_find` at line 788; `cargo fmt -p frankenlibc-core --check` and `git diff --check -- crates/frankenlibc-core/src/string/str.rs` passed locally.
- Score: Impact 3 x Confidence 5 / Effort 1 = 15.0, kept.

---

Completion update for `bd-2g7oyh.24`: malloc lifecycle hit-rate cache candidate rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::record_lifecycle`.
- Baseline recorded in bead on worker `vmi1156319`: `malloc_free_64` FrankenLibC p50 `652.935 ns/op`, p95 `987.176`, p99 `1403.000`, mean `643.201`; host glibc p50 `8.502`, p95 `20.000`, p99 `151.000`.
- Candidate lever: cache `cache_hit_rate_permille` after hit/miss counter changes instead of recomputing a division for every lifecycle record.
- Score before measurement: Impact 2 x Confidence 4 / Effort 1 = 8.0.

Pass 2 - Golden behavior: complete.
- RCH focused golden command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core malloc::allocator::tests::lifecycle_hit_rate_permille_transcript_sha256 -- --nocapture`.
- RCH golden result on `vmi1156319`: passed.
- Candidate transcript SHA256: `ca9a28b5d24ed89160a9de3a51d9ab8346f01fa0e4368e2045deb658159b5e60`.

Pass 3 - Isomorphism proof: complete.
- Record order/count, decision ids, trace IDs, details text, active allocation counters, hit/miss counters, and exact `cache_hit_rate_permille` snapshots were intended to remain unchanged.
- Tie-breaking and allocator reuse policy were unchanged; floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- Post benchmark command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench glibc_baseline_bench glibc_baseline_malloc_free_64 -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Post benchmark on worker `vmi1153651`: FrankenLibC p50 `748.178 ns/op`, p95 `1319.254`, p99 `1535.430`, mean `861.029`; host p50 `9.423`, p95 `16.950`, p99 `40.000`.
- Verdict: failed Score>=2.0 keep gate; code reverted/discarded, no behavior change retained.

---

Completion update for `bd-2g7oyh.26`: wcsrchr safe-SIMD clean-panel skips.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/string/wide.rs::wcsrchr`, absent 4096-codepoint scan.
- RCH baseline on worker `vmi1227854`: `wcsrchr_absent_4096` p50 `1189.980 ns/op`, p95 `1426.600`, p99 `1550.883`.
- Source evidence: the previous path compared eight lanes manually but still touched every clean panel; sibling wide scans already used `has_wide_or_nul_simd` as a safe panel classifier.
- Score before implementation: Impact 3 x Confidence 4 / Effort 1 = 12.0.

Pass 2 - Golden behavior: complete.
- Pre-change RCH focused wide tests passed 6 tests with test-line SHA256 `fd2bde19273a0dea5b7c0b6526135f3470f4eeebc5764f5b259eb9a34043e258`.
- Post-change RCH focused wide tests passed 7 tests with full transcript SHA256 `752ec9832776905e24b2c633be4b01612d4d137146c0121ed22a194ee0014a8a`.
- Final wide-string RCH sweep passed 73/73 tests after the hygiene fixes.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: SIMD group probing over a packed control plane, specialized to skip panels that contain neither the target codepoint nor NUL.
- Ordering is preserved because the new SIMD predicate only skips clean panels; target or NUL panels still enter scalar left-to-right resolution.
- Tie-breaking and last-match behavior are unchanged because candidate updates still happen in scalar index order inside each non-clean panel.
- NUL termination semantics are unchanged because any panel containing NUL is not skipped, and the scalar path still stops there.
- Floating-point and RNG behavior are unaffected.

Pass 4 - One-lever implementation: complete.
- Lever: replaced the manual eight-lane `wcsrchr` skip predicate with `chunks_exact(WIDE_SIMD_LANES)` plus `has_wide_or_nul_simd`.
- Added `test_wcsrchr_panel_stops_at_nul_before_later_match`.
- Validation hygiene in the same file removed an unused `base` binding and rewrote the `wmemrchr` tail as `iter().rposition`-style logic to satisfy clippy without changing search semantics.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark on worker `vmi1149989`: p50 `658.641 ns/op`, p95 `1240.550`, p99 `1773.000`.
- Confirmation post benchmark on worker `vmi1293453`: p50 `531.000 ns/op`, p95 `629.081`, p99 `1017.782`; speedup `1189.980 -> 531.000 ns/op` = 2.24x.
- Validation: RCH `cargo check -p frankenlibc-core --all-targets` passed; RCH `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed; local `cargo fmt --check -p frankenlibc-core`, `git diff --check`, and UBS critical scan passed.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0, kept in commits `cca9473b` and `0b9a97d1`.

---

Completion update for `bd-psvhnc`: AVX2/FMA target-feature floor.

Pass 1 - Profile-backed target selection: complete.
- Target: project-wide residual width gap across safe portable-SIMD string and memory scans.
- RCH baseline on worker `vmi1156319`: `strlen_4096` p50 `71.128 ns/op`, p95 `99.414`, p99 `160.000`; `memchr_absent_4096` p50 `110.715`, p95 `129.913`, p99 `140.000`.
- Bead-carried same-worker evidence from the cc agent showed `strlen_4096 70.4 -> 25.1 ns` and `memchr_absent_4096 71.5 -> 38.4 ns` with AVX2 enabled.
- Score before implementation: Impact 5 x Confidence 4 / Effort 1 = 20.0.

Pass 2 - Golden behavior: complete.
- Pre-change RCH string tests passed 397/397 with stable `test ... ok` line SHA256 `007b3407c902798030eb31a0710913baaf9688c2205ab34ec652a278c4537c17`.
- Post-change RCH string tests passed 397/397 with the same stable SHA256 `007b3407c902798030eb31a0710913baaf9688c2205ab34ec652a278c4537c17`.

Pass 3 - Alien primitive selection and isomorphism proof: complete.
- Graveyard primitive: SIMD group probes over packed control bytes, generalized to a project-wide AVX2-capable vector-width floor for the existing safe-Rust SIMD kernels.
- Ordering, tie-breaking, first/last-match behavior, NUL handling, bounds behavior, and public API contracts are unchanged because no Rust source control flow changed.
- Floating-point semantics are unchanged: Rust does not enable fast-math contraction for ordinary `a * b + c`; `+fma` only makes fused instructions available where Rust semantics already request them.
- RNG behavior is unchanged; no RNG code, seeds, ordering, or state transitions changed.

Pass 4 - One-lever implementation: complete.
- Lever: added `.cargo/config.toml` with `[build] rustflags = ["-Ctarget-feature=+avx2,+fma"]`.
- No Rust source changed, no unsafe target-feature function was introduced, and no C BLAS/LAPACK/MKL/XLA linkage was added.
- Contract change: produced x86-64 binaries now require AVX2/FMA-capable CPUs, matching the owner-approved Option A recorded in `bd-psvhnc`.

Pass 5 - Re-benchmark and validation: complete.
- Post benchmark on worker `vmi1293453`: `strlen_4096` p50 `18.742 ns/op`, p95 `22.986`, p99 `45.000`; `memchr_absent_4096` p50 `38.514`, p95 `50.000`, p99 `70.000`.
- Before/after p50: `strlen_4096 71.128 -> 18.742 ns/op` = 3.80x; `memchr_absent_4096 110.715 -> 38.514 ns/op` = 2.88x.
- Validation: RCH `cargo check -p frankenlibc-core -p frankenlibc-bench --all-targets` passed; RCH `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed; combined bench clippy remains blocked by pre-existing literal-cast lints in `benches/string_bench.rs:777-778`.
- Score: Impact 5 x Confidence 4 / Effort 1 = 20.0, kept in commit `3f4c676f`.

---

Completion update for `bd-2g7oyh.27`: qsort direct pivot-slice candidate rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/stdlib/sort.rs::partition`, `qsort_128_i32`.
- Fresh post-AVX2 RCH comparison on worker `vmi1227854`: FrankenLibC p50 `3016.798 ns/op`, p95 `3506.000`, p99 `3521.529`; host glibc p50 `2353.532`, p95 `2667.644`, p99 `2677.485`.
- Focused pre-edit RCH baseline on worker `vmi1227854`: FrankenLibC p50 `2876.385 ns/op`, p95 `3468.408`, p99 `3706.239`; host glibc p50 `2305.538`, p95 `2747.562`, p99 `3065.000`.
- Malloc was the larger gap, but allocator/thread-cache/elimination files were already dirty with unrelated work, so qsort was the clean claimable profiler-evident target.
- Score before implementation: Impact 1 x Confidence 3 / Effort 1 = 3.0.

Pass 2 - Golden behavior: complete.
- Pre-change RCH focused qsort tests passed 2/2 on worker `vmi1156319`.
- Post-change RCH focused qsort tests passed with the same stable test-line SHA256: `350db3380805adf9e7f68399c59510fdd3ea76323c3f3873e772c5456aa277a5`.

Pass 3 - Isomorphism proof: complete.
- Candidate changed only how the immutable current-element and pivot slices were obtained inside `partition`.
- Partition scan order, comparator call order, `cmp <= 0` tie handling, swap sequence, pivot placement, recursion order, floating-point behavior, and RNG behavior were unchanged.

Pass 4 - One-lever implementation: rejected.
- Candidate lever: cache `pivot_start`/`pivot_end` and use direct immutable slices instead of rebuilding `head`/`tail` with `split_at` on every partition comparison.
- No C linkage, no unsafe, and no benchmark harness changes.

Pass 5 - Re-benchmark and keep/reject decision: rejected.
- First post RCH on worker `vmi1149989`: FrankenLibC p50 `2274.069 ns/op`, p95 `3444.740`, p99 `3450.804`; host glibc p50 `1900.033`.
- Confirmation post RCH on the same post worker `vmi1149989`: FrankenLibC p50 `3335.000 ns/op`, p95 `3581.447`, p99 `4097.253`; host glibc p50 `2131.603`.
- Verdict: failed the real-win/Score>=2.0 keep gate because the confirmation regressed relative to the focused baseline and worsened ratio-to-host. Source was restored to HEAD; no `sort.rs` change retained.

---

Completion update for `bd-2g7oyh.30`: malloc size-class certificate fixed-format details.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::MallocState::malloc`, hot 64-byte size-class certificate detail formatting.
- Fresh post-typed-index broad RCH profile on worker `vmi1153651`: `malloc_free_64` FrankenLibC p50 `874.552 ns/op`, p95 `1724.366`, p99 `1964.000`, mean `968.670`; host p50 `9.383`, p95 `18.564`, p99 `35.500`.
- Controlled pre-change focused RCH baseline on worker `vmi1293453`: FrankenLibC p50 `321.154 ns/op`, p95 `361.590`, p99 `415.500`; host p50 `4.400`, p95 `6.875`, p99 `35.000`.
- Score before implementation: Impact 3 x Confidence 4 / Effort 1 = 12.0.

Pass 2 - Golden behavior: complete.
- Pre-change RCH exact lifecycle golden passed with normalized test-line SHA256 `99efb08f2b612ec70e03fe51ee73620e2965a682de888e3e16cf0c3de360a564`.
- Post-change RCH exact lifecycle golden passed with the same SHA256 `99efb08f2b612ec70e03fe51ee73620e2965a682de888e3e16cf0c3de360a564`.

Pass 3 - Isomorphism proof: complete.
- Ordering is preserved because the `size_class_certificate` lifecycle record is still emitted at the same point before the thread-cache allocation attempt.
- Tie-breaking is unchanged because bin selection, certificate pass/violation outcome, thread-cache order, central-bin order, and free path behavior are unchanged.
- Floating-point and RNG behavior are unaffected; the edit only selects a byte-identical static string for the profiled integer triple and keeps the original fallback formatting for all other triples.

Pass 4 - One-lever implementation: complete.
- Lever: replace the profiled `size=64`, `class_size=64`, `cert_value=150000` generic `format!` detail construction with a byte-identical static string path; all other triples keep the original `format!` fallback.
- No allocator policy changes, no benchmark harness changes, no unsafe code, no C linkage, and no edits to dirty `malloc/elimination.rs`.

Pass 5 - Re-benchmark and validation: complete.
- Post RCH benchmark on worker `vmi1227854`: FrankenLibC p50 `227.763 ns/op`, p95 `256.981`, p99 `337.748`, mean `223.498`; host p50 `4.403`, p95 `6.281`, p99 `30.000`.
- Before/after: p50 `321.154 -> 227.763 ns/op` = 1.41x; p95 `361.590 -> 256.981` = 1.41x; p99 `415.500 -> 337.748` = 1.23x. Host p50 stayed comparable at `4.400 -> 4.403 ns/op`.
- Validation: RCH `cargo test -p frankenlibc-core malloc::allocator::tests:: --lib -- --test-threads=1` passed 14/14; RCH `cargo check -p frankenlibc-core --all-targets` passed; RCH `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed.
- Formatting: RCH refused `cargo fmt` as non-compilation under remote-only; local package `cargo fmt --check -p frankenlibc-core` is blocked by unrelated `strstr_golden_corpus_test.rs:69`; touched-file `rustfmt --edition 2024 --check crates/frankenlibc-core/src/malloc/allocator.rs` and `git diff --check` passed.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0, kept.

---

Completion update for `bd-2g7oyh.32`: malloc cache-hit-rate derived snapshot candidate rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::record_lifecycle` cache-hit-rate snapshot.
- Profile evidence in bead: post-`66f494a1`/`0dafc72b` RCH `malloc_free_64` on `vmi1167313` at FrankenLibC p50 `561.261 ns/op`, p95 `850.736`, p99 `969.135`; host p50 `8.880`.
- Candidate lever: cache `cache_hit_rate_permille` inside `MallocState` and refresh only after thread-cache hit/miss counters change.
- Score before measurement: Impact 2 x Confidence 4 / Effort 1 = 8.0.

Pass 2 - Baseline and golden behavior: complete.
- Controlled pre-change RCH benchmark with the candidate diff removed on `vmi1227854`: FrankenLibC p50 `242.360 ns/op`, p95 `277.292`, p99 `286.346`; host p50 `3.768`.
- Pre-change RCH exact lifecycle golden passed on `vmi1167313`.
- Pinned lifecycle golden SHA256: `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof: complete.
- Ordering was preserved because lifecycle record emission points and event order were unchanged.
- Tie-breaking was unchanged because allocation path selection, thread-cache LIFO order, central-bin order, elimination behavior, and pointer reuse were unchanged.
- Floating-point and RNG behavior were unaffected.
- Post-change RCH exact lifecycle golden passed on `vmi1293453` with the same pinned lifecycle SHA.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- Post-change RCH benchmark on `vmi1293453`: FrankenLibC p50 `256.196 ns/op`, p95 `323.097`, p99 `550.000`; host p50 `3.805`.
- Verdict: failed the real-win/Score>=2.0 keep gate because p50 regressed (`242.360 -> 256.196 ns/op`) and tails worsened.
- Source restored: no `allocator.rs` change retained.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.32-malloc-cache-hit-rate.md`.

---

Completion update for `bd-2g7oyh.33`: malloc 64 certificate details borrowed static hot string candidate rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::size_class_certificate_details`.
- Profile evidence: fresh RCH `glibc_baseline_bench` after `bd-2g7oyh.32` rejection measured `malloc_free_64` on `vmi1149989` at FrankenLibC p50 `353.242 ns/op`, p95 `561.802`, p99 `878.315`; host p50 `5.312`.
- Source evidence: the profiled hot certificate triple still returned `Cow::Owned(HOT_CERT_64_DETAILS.to_owned())`, allocating a byte-identical static string each hot cycle.
- Candidate lever: return `Cow::Borrowed(HOT_CERT_64_DETAILS)` for exactly `size=64`, `class_size=64`, `cert_value=150000`; retain the existing formatted owned fallback for every other triple.
- Score before measurement: Impact 1 x Confidence 4 / Effort 1 = 4.0.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH benchmark on `vmi1149989`: FrankenLibC p50 `194.436 ns/op`, p95 `391.806`, p99 `486.828`, mean `207.648`; host p50 `3.774`, p95 `10.000`, p99 `30.000`, mean `5.691`.
- Pre-change RCH hot-cycle golden tests passed on `vmi1227854`.
- Pinned lifecycle golden SHA256: `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof and implementation: complete.
- Ordering is preserved because the certificate lifecycle record is emitted at the same point before thread-cache allocation.
- Tie-breaking is unchanged because size-class selection, certificate outcome, thread-cache LIFO, central-bin order, and elimination behavior are unchanged.
- Log detail bytes are unchanged; the attempted observable ownership change was only `Cow::Owned` to `Cow::Borrowed` for the exact static hot detail.
- Floating-point and RNG behavior are unaffected.
- Post-change RCH hot-cycle golden tests passed on `vmi1227854` with the same pinned lifecycle SHA.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- Post RCH benchmark on `vmi1293453`: FrankenLibC p50 `233.949 ns/op`, p95 `270.132`, p99 `381.277`, mean `230.552`; host p50 `4.286`, p95 `13.323`, p99 `40.000`, mean `7.001`.
- Normalized ratio regressed from `51.52x` baseline to `54.58x` post, so Score < 2.0.
- Source restored: no `allocator.rs` change retained.

---

Completion update for `bd-2g7oyh.34`: malloc free single-owner elimination skip.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::MallocState::free`, 64-byte allocate/free cycle.
- Fresh post-`dfe3d335` RCH broad profile on worker `vmi1156319`: `malloc_free_64` FrankenLibC p50 `494.034 ns/op`, p95 `536.822`, p99 `649.108`, mean `486.069`; host p50 `8.655`, p95 `14.899`, p99 `45.000`.
- Focused old-path RCH baseline on worker `vmi1153651`: FrankenLibC p50 `503.022 ns/op`, p95 `721.973`, p99 `1009.732`, mean `521.706`; host p50 `9.321`, p95 `16.240`, p99 `40.000`, mean `12.600`.
- Candidate lever: skip `elimination.try_offer` when `Arc::strong_count(&self.elimination) == 1`.
- Score before implementation: Impact 2 x Confidence 3 / Effort 1 = 6.0.

Pass 2 - Golden behavior: complete.
- Pre-change RCH exact hot-cycle lifecycle golden passed.
- Post-change RCH exact hot-cycle lifecycle golden passed.
- Pinned lifecycle golden SHA256: `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof and implementation: complete.
- Ordering is preserved when an external elimination consumer can exist because cloned handles make `Arc::strong_count > 1`, retaining the old elimination-first free path.
- Tie-breaking is unchanged because the same slot bias, pointer value, metadata formatting, and early return path are used whenever a matching consumer is possible.
- Single-owner behavior is unchanged because no waiting consumer can reach the elimination array without another `Arc`; the previous path could only fall back with the same pointer before the same thread-cache/central-bin/backend logic.
- Lifecycle record bytes stayed unchanged on the hot cycle; floating-point and RNG behavior are unaffected.
- One lever only: no `malloc/elimination.rs` changes, no harness changes, no unsafe code, no C linkage.

Pass 4 - Re-benchmark and validation: complete.
- First post RCH benchmark on worker `vmi1293453`: FrankenLibC p50 `160.995 ns/op`, p95 `203.031`, p99 `224.241`, mean `173.149`; host p50 `4.224`, p95 `8.219`, p99 `35.500`, mean `7.085`.
- Confirmation post RCH benchmark on worker `vmi1156319`: FrankenLibC p50 `385.510 ns/op`, p95 `467.191`, p99 `1102.000`, mean `397.912`; host p50 `8.726`, p95 `14.993`, p99 `172.750`, mean `16.741`.
- Host-normalized p50 ratio improved from `53.97x` focused old-path baseline to `38.11x` and `44.18x`; raw p50 kept result was `503.022 -> 385.510 ns/op`, with best observed post `160.995 ns/op`.
- Validation: RCH `cargo test -p frankenlibc-core malloc::allocator::tests:: --lib -- --test-threads=1` passed 14/14; RCH `cargo check -p frankenlibc-core --all-targets` passed; RCH `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed; touched-file `rustfmt --edition 2024 --check crates/frankenlibc-core/src/malloc/allocator.rs` and `git diff --check` passed locally.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.34-malloc-single-owner-elimination.md`.
- Score: Impact 2 x Confidence 4 / Effort 1 = 8.0, kept.

---

Completion update for `bd-2g7oyh.35`: malloc 64 hot certificate constant value.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs` certificate value on the `malloc_free_64` hot tuple.
- Fresh post-`bd-2g7oyh.34` RCH profile on `vmi1149989`: `malloc_free_64` FrankenLibC p50 `143.209 ns/op`, p95 `179.575`, p99 `208.400`, mean `144.791`; host p50 `3.297`, p95 `6.938`, p99 `30.000`.
- Micro-profile baseline on `vmi1156319`: `sos_barrier_size_class_eval_with_lookup` p50 `20.902 ns/op`, p95 `22.770`, p99 `45.500`, mean `23.491`.
- Candidate lever: return `150000` directly for exactly `(requested_size=64, mapped_class_size=64, class_membership_valid=true)`; delegate every other tuple to `evaluate_size_class_barrier`.
- Score before implementation: Impact 1 x Confidence 4 / Effort 1 = 4.0.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH malloc baseline on `vmi1156319`: FrankenLibC p50 `387.141 ns/op`, p95 `487.764`, p99 `1052.977`, mean `400.542`; host p50 `8.729`, p95 `14.024`, p99 `40.500`.
- RCH allocator tests passed 15/15 on `vmi1227854`.
- Pinned lifecycle golden SHA256 stayed `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof and implementation: complete.
- Ordering is preserved because the certificate lifecycle record is emitted at the same point before thread-cache allocation.
- Tie-breaking is unchanged because size-class selection, certificate pass/violation outcome, thread-cache LIFO, central-bin order, elimination behavior, and pointer reuse are unchanged.
- Integer certificate behavior is unchanged for the hot tuple because `evaluate_size_class_barrier(64, 64, true) == 150000`.
- Non-hot tuple behavior is unchanged because the helper delegates to the runtime SOS evaluator.
- Floating-point and RNG behavior are unaffected.

Pass 4 - Re-benchmark and validation: complete.
- First post RCH benchmark on `vmi1156319`: FrankenLibC p50 `376.930 ns/op`, p95 `444.856`, p99 `1122.000`, mean `391.897`; host p50 `8.590`.
- Confirmation post RCH benchmark on `vmi1149989`: FrankenLibC p50 `130.110 ns/op`, p95 `195.371`, p99 `290.000`, mean `147.925`; host p50 `2.971`.
- Same-worker p50 improved `387.141 -> 376.930 ns/op`; same-worker p95 improved `487.764 -> 444.856 ns/op`; current-HEAD profile worker p50 improved `143.209 -> 130.110 ns/op`.
- Validation: RCH `cargo test -p frankenlibc-core malloc::allocator::tests:: --lib -- --test-threads=1 --nocapture` passed; RCH `cargo check -p frankenlibc-core --all-targets` passed; RCH `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed; local touched-file rustfmt and `git diff --check` passed.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.35-malloc-hot-cert-fast-path.md`.
- Score: Impact 1 x Confidence 4 / Effort 1 = 4.0, kept.

---

Completion update for `bd-2g7oyh.36`: malloc 64 hot bin/class shortcut candidate rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs` 64-byte size-class lookup on `malloc_free_64`.
- Fresh post-`bd-2g7oyh.35` RCH profile on `vmi1227854`: `malloc_free_64` FrankenLibC p50 `177.986 ns/op`, p95 `221.535`, p99 `306.625`, mean `185.595`; host p50 `3.571`, p95 `4.897`, p99 `25.000`.
- Candidate lever: add/use a constant 64-byte size-class index and class size for the exact hot path; delegate non-64 sizes to the existing generic lookup.
- Score before measurement: Impact 1 x Confidence 3 / Effort 1 = 3.0.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH malloc baseline on `vmi1227854`: FrankenLibC p50 `166.312 ns/op`, p95 `199.002`, p99 `546.168`, mean `175.132`; host p50 `4.441`, p95 `10.000`, p99 `45.000`.
- Pre-change RCH exact lifecycle golden passed on `vmi1153651`.
- Post-change RCH exact lifecycle golden passed on `vmi1293453`.
- Pinned lifecycle golden SHA256 stayed `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof: complete.
- Ordering would be preserved because lifecycle emission points were unchanged.
- Tie-breaking would be unchanged because allocation/free path order, thread-cache LIFO, central-bin order, elimination behavior, and pointer reuse were unchanged.
- Integer size-class behavior would be unchanged for the hot tuple because 64 bytes still mapped to bin `3`, class size `64`, and certificate value `150000`; non-hot sizes delegated to the generic lookup.
- Floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- First post RCH benchmark on `vmi1153651`: FrankenLibC p50 `363.191 ns/op`, p95 `437.874`, p99 `791.000`, mean `375.272`; host p50 `8.781`.
- Confirmation post RCH benchmark on `vmi1153651`: FrankenLibC p50 `362.618 ns/op`, p95 `451.000`, p99 `583.733`, mean `374.936`; host p50 `8.776`.
- Verdict: failed the real-win/Score>=2.0 keep gate because raw p50 regressed and same-worker confirmation repeated the regression.
- Source restored: no `allocator.rs` or `size_class.rs` change retained.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.36-malloc-hot-bin-shortcut.md`.
- Score after measurement: 0.0, rejected.

---

Completion update for `bd-2g7oyh.37`: malloc lifecycle trace prefix fast path candidate rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs` lifecycle trace-id prefix construction on `malloc_free_64`.
- Fresh post-`bd-2g7oyh.36` broad RCH profile on `vmi1227854`: `malloc_free_64` FrankenLibC p50 `177.986 ns/op`, p95 `221.535`, p99 `306.625`, mean `185.595`; host p50 `3.571`, p95 `4.897`, p99 `25.000`.
- Candidate lever: use static trace-id prefixes for hot lifecycle symbols `malloc`, `free`, and `size_class_certificate`; preserve generic fallback and fixed lowercase-hex suffix for all other cases.
- Score before measurement: Impact 1 x Confidence 3 / Effort 1 = 3.0.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH malloc baseline on `vmi1153651`: FrankenLibC p50 `368.140 ns/op`, p95 `416.938`, p99 `528.365`, mean `373.502`; host p50 `9.019`, p95 `16.250`, p99 `90.000`.
- Post-change RCH allocator tests passed 15/15 on `vmi1149989`.
- Pinned lifecycle golden SHA256 stayed `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof: complete.
- Ordering would be preserved because lifecycle records were still emitted at the same allocation/free/certificate points.
- Trace decision IDs and bytes would be unchanged because the candidate only changed construction of equal hot-prefix bytes before the same fixed lowercase-hex suffix.
- Tie-breaking would be unchanged because size-class selection, thread-cache LIFO, central-bin order, elimination behavior, and pointer reuse were not touched.
- Floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- First post RCH benchmark on `vmi1153651`: FrankenLibC p50 `364.070 ns/op`, p95 `537.303`, p99 `681.000`, mean `369.638`; host p50 `8.965`.
- Confirmation post RCH benchmark on `vmi1153651`: FrankenLibC p50 `398.683 ns/op`, p95 `2635.527`, p99 `7603.309`, mean `725.463`; host p50 `8.805`.
- Verdict: failed the real-win/Score>=2.0 keep gate because same-worker confirmation regressed p50/mean and materially worsened tail latency.
- Source restored: no `allocator.rs` change retained.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.37-malloc-trace-prefix-fast-path.md`.
- Score after measurement: 0.0, rejected.

---

Completion update for `bd-2g7oyh.38`: qsort width-4 swap specialization candidate rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/stdlib/sort.rs` `swap_chunks` on `qsort_128_i32`.
- Fresh post-`bd-2g7oyh.37` broad RCH profile on `vmi1149989`: `qsort_128_i32` FrankenLibC p50 `2410.516 ns/op`, p95 `3458.206`, p99 `3540.895`, mean `2559.625`; host p50 `1988.379`, p95 `2569.028`, p99 `2592.369`.
- Candidate lever: specialize `swap_chunks` for `width == 4`; preserve the generic path for all other widths.
- Alien primitive: fixed-width/register micro-kernel specialization for a hot inner loop.
- Score before measurement: Impact 1 x Confidence 3 / Effort 1 = 3.0.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH qsort baseline on `vmi1153651`: FrankenLibC p50 `7751.337 ns/op`, p95 `13340.911`, p99 `16677.281`, mean `8563.939`; host p50 `4541.030`, p95 `5926.439`, p99 `6534.256`.
- Pre-change RCH golden SHA test passed on `vmi1227854`.
- Post-change RCH golden SHA test passed on `vmi1149989`.
- Pinned qsort reverse-ish golden SHA256 stayed `6f1d71d33c28656abb499ebd7177b9eda2ee8d3d369d1fb3f4e131944932851a`.

Pass 3 - Isomorphism proof: complete.
- Compare ordering would be preserved because partitioning, recursion, pivot selection, and comparator calls were untouched.
- Equality tie behavior would be preserved because the same element swap sites would occur; only byte movement inside each width-4 swap changed.
- Non-4-byte element sorting would be unchanged by direct delegation to the original generic path.
- Floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- First post RCH benchmark on `vmi1227854`: FrankenLibC p50 `2735.616 ns/op`, p95 `3105.548`, p99 `3150.235`, mean `2743.964`; host p50 `2272.653`.
- Confirmation post RCH benchmark on `vmi1227854`: FrankenLibC p50 `2958.296 ns/op`, p95 `3367.273`, p99 `3671.713`, mean `2957.595`; host p50 `2615.785`.
- Verdict: failed the real-win/Score>=2.0 keep gate because same-worker post confirmation did not show a stable win.
- Source restored: no `sort.rs` change retained.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.38-qsort-width4-swap-specialization.md`.
- Score after measurement: 0.0, rejected.

---

Completion update for `bd-2g7oyh.40`: memset fill kernel pass rejected/no-op.

Pass 1 - Profile-backed target selection: complete.
- Target considered: `crates/frankenlibc-core/src/string/mem.rs::memset` on `glibc_baseline_memset_4096`.
- Prior broad RCH profile showed a small `memset_4096` gap: FrankenLibC p50 `19.526 ns/op`, mean `20.996`; host p50 `17.094`, mean `18.493`.
- Coordination: skipped `str.rs` because BlackThrush owns the active strlen residual lane; skipped allocator because allocator surfaces are peer-dirty/contended.
- Alien primitive: vectorized hot-operator/cache-local fill paneling, with exact prefix-fill behavior as the proof contract.

Pass 2 - Focused RCH baseline: complete.
- Focused command: `RCH_REQUIRE_REMOTE=1 RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 CARGO_TARGET_DIR=/data/tmp/rch_target_frankenlibc_bd_2g7oyh_40_memset_baseline cargo bench -p frankenlibc-bench --bench glibc_baseline_bench glibc_baseline_memset_4096 -- --sample-size 50 --measurement-time 3 --warm-up-time 1 --noplot`.
- Worker `vmi1264463`: FrankenLibC p50 `58.740 ns/op`, p95 `335.486`, p99 `744.551`, mean `102.433`; host p50 `75.130`, p95 `536.762`, p99 `648.463`, mean `131.449`.

Pass 3 - Keep/reject decision: rejected/no-op.
- Verdict: no current profile-backed vs-host gap on the focused baseline; FrankenLibC was faster than host on p50, p95, and mean for this row.
- Source unchanged: no `mem.rs` lever applied.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.40-memset-fill.md`.
- Score after measurement: 0.0, rejected/no-op.

---

Completion update for `bd-2g7oyh.41`: malloc elimination external-handle gate rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs` per-free elimination handle gate on `malloc_free_64`.
- Fresh post-`27e332a7` broad RCH profile: `malloc_free_64` FrankenLibC p50 `163.665 ns/op`, p95 `221.719`, p99 `328.539`, mean `174.630`; host p50 `4.810`, p95 `10.000`, p99 `40.000`, mean `7.049`.
- Candidate lever: skip production `Arc::strong_count(&self.elimination)` while preserving the exact `strong_count > 1` gate under `cfg(test)`.
- Coordination: did not touch peer-dirty `crates/frankenlibc-core/src/malloc/elimination.rs`.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH malloc baseline on `vmi1149989`: FrankenLibC p50 `156.635 ns/op`, p95 `189.048`, p99 `300.438`, mean `154.100`; host p50 `4.139`, p95 `8.750`, p99 `35.000`, mean `5.890`.
- Pre-change exact lifecycle SHA test passed on `vmi1149989`.
- Post-change exact lifecycle SHA test passed on `vmi1293453`.
- Pinned lifecycle golden SHA256 stayed `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof: complete.
- Production ordering would remain unchanged because the candidate only skipped an impossible external-handle check before the existing thread-cache and central-bin paths.
- Test ordering would remain unchanged because `cfg(test)` retained the exact elimination-first gate.
- Tie-breaking was unchanged because size-class selection, thread-cache LIFO, central-bin order, and elimination slot logic were not modified.
- Floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- Post RCH benchmark on `vmi1293453`: FrankenLibC p50 `156.085 ns/op`, p95 `191.135`, p99 `196.388`, mean `157.231`; host p50 `3.661`, p95 `6.250`, p99 `25.000`, mean `5.036`.
- Verdict: failed the real-win/Score>=2.0 keep gate because p50 changed by only `0.550 ns/op` and mean regressed.
- Source restored: no `allocator.rs` change retained.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.41-malloc-elimination-handle-gate.md`.
- Score after measurement: 0.0, rejected.

---

Completion update for `bd-2g7oyh.42`: malloc lifecycle drain swap rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::drain_lifecycle_logs` on `malloc_free_64`.
- Fresh post-`b50a99ee` broad RCH profile on `vmi1293453`: `malloc_free_64` FrankenLibC p50 `164.641 ns/op`, p95 `183.011`, p99 `270.324`, mean `173.146`; host p50 `3.719`, p95 `5.000`, p99 `30.000`, mean `5.223`.
- Coordination: skipped BlackThrush's active `str.rs` strlen/strcmp lane and left peer-dirty `malloc/elimination.rs` untouched.
- Candidate lever: replace `drain(..).collect()` with a backing-Vec swap that preserves returned record order and leaves the logger with the previous capacity.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH malloc baseline on `vmi1149989`: FrankenLibC p50 `128.725 ns/op`, p95 `226.868`, p99 `253.176`, mean `147.439`; host p50 `3.117`, p95 `4.771`, p99 `30.000`, mean `4.548`.
- Pre-change allocator proof passed 15/15 tests on `vmi1293453`.
- Post-change allocator proof passed 15/15 tests on `vmi1264463`.
- Pinned lifecycle golden SHA256 stayed `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof: complete.
- Drain record ordering would be unchanged because the candidate moved the whole backing vector without reordering elements.
- Post-drain retained capacity was unchanged by the allocator drain test.
- Allocation ordering and tie-breaking were unchanged because size-class selection, thread-cache LIFO, central-bin order, elimination behavior, comparator paths, FP, and RNG were not modified.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- Post RCH benchmark on `vmi1264463`: FrankenLibC p50 `402.791 ns/op`, p95 `783.520`, p99 `1001.594`, mean `468.792`; host p50 `9.541`, p95 `25.000`, p99 `31.667`, mean `12.336`.
- Verdict: failed the real-win/Score>=2.0 keep gate because p50 and mean materially regressed.
- Source restored: no `allocator.rs` change retained.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.42-malloc-lifecycle-drain-swap.md`.
- Score after measurement: 0.0, rejected.

---

Completion update for `bd-2g7oyh.44`: memcpy exact full-slice fast path rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/string/mem.rs::memcpy` on `memcpy_4096`.
- Fresh post-`bf9330ff` broad RCH profile on `vmi1293453`: `memcpy_4096` FrankenLibC p50 `41.868 ns/op`, p95 `46.645`, p99 `65.000`, mean `43.545`; host p50 `27.074`, p95 `32.500`, p99 `90.000`, mean `29.827`.
- Coordination: skipped BlackThrush's active `str.rs` strlen/strcmp lane and left peer-dirty allocator surfaces untouched.
- Candidate lever: add a safe exact full-slice fast path for `n == dest.len() == src.len()` before the existing clamped prefix-copy path.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH memcpy baseline on `vmi1293453`: FrankenLibC p50 `41.456 ns/op`, p95 `45.160`, p99 `80.000`, mean `43.312`; host p50 `31.300`, p95 `33.707`, p99 `40.000`, mean `31.379`.
- Pre-change string memory proof passed 41/41 tests on `vmi1156319`.
- Post-change string memory proof passed 41/41 tests on `vmi1149989`.
- Golden fixture SHA256 values stayed `string_memory_full.json=94e8dc73391d2f0d29fa07dc15366150bc015b59ce2d7c2e18ead6373f35b9e4` and `memcpy_strict.json=6bdd6fb00bff508d07eb985bdc7c258a1a10f8ea96de72cf7e392483e886c233`.

Pass 3 - Isomorphism proof: complete.
- Exact full-slice cases would return the same `n` and copy the same complete slice because the original `count` also equals `n`.
- Partial cases would fall through to the original `min(n, dest.len(), src.len())` prefix-copy path, preserving copied prefix and untouched tail behavior.
- Ordering and tie-breaking were unchanged because `memcpy` performs no comparisons and preserves the same non-overlap copy contract.
- Floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- Post RCH benchmark on `vmi1227854`: FrankenLibC p50 `42.652 ns/op`, p95 `46.620`, p99 `75.000`, mean `45.301`; host p50 `28.231`, p95 `31.184`, p99 `50.000`, mean `29.598`.
- Verdict: failed the real-win/Score>=2.0 keep gate because p50 and mean regressed.
- Source restored: no `mem.rs` change retained; restored source SHA256 `8e7674c7176db872361de4f1038003eacd87fc9c510047f6f7aa42d4d218fd08`.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.44-memcpy-exact-full-slice.md`.
- Score after measurement: 0.0, rejected.

---

Completion update for `bd-2g7oyh.45`: qsort duplicate-heavy three-way partition rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/stdlib/sort.rs::qsort` on `qsort_128_i32`.
- Fresh post-`87695585` broad RCH profile on `vmi1167313`: `qsort_128_i32` FrankenLibC p50 `7182.823 ns/op`, p95 `7910.732`, p99 `8754.439`, mean `7070.474`; host p50 `4556.657`, p95 `5316.742`, p99 `5709.379`, mean `4499.363`.
- Coordination: skipped BlackThrush's active `str.rs` strlen lane and left peer-dirty allocator surfaces untouched.
- Candidate lever: replace the two-way Lomuto qsort partition recursion with a duplicate-aware three-way partition for `< pivot`, `== pivot`, and `> pivot` bands.

Pass 2 - Baseline and golden behavior: complete.
- Focused pre-change RCH qsort baseline on `vmi1264463`: FrankenLibC p50 `7341.765 ns/op`, p95 `15465.841`, p99 `18053.184`, mean `8257.883`; host p50 `4724.885`, p95 `8558.040`, p99 `10761.861`, mean `5297.461`.
- Pre-change sort proof passed 25/25 tests on `vmi1153651`.
- Post-change sort proof passed 26/26 tests on `vmi1153651`, including an all-equal qsort regression.
- Golden fixture SHA256 stayed `stdlib_sort.json=ff7eb36ea363367e78b7287e53ecbd28ef43f0fed8337c864d446ae166be71b6`.

Pass 3 - Isomorphism proof: complete.
- Sorted multiset output would be preserved because every element is still compared by the caller comparator and partitioned into less/equal/greater bands before recursive sorting.
- Duplicate tie behavior is not a qsort stability guarantee; equal-element ordering remains unspecified.
- Error classes and zero-width/short-buffer early exits were unchanged because only the recursive partition path was edited.
- Floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- Post RCH benchmark on `vmi1153651`: FrankenLibC p50 `8410.067 ns/op`, p95 `9507.546`, p99 `10046.258`, mean `8186.175`; host p50 `4572.499`, p95 `5806.553`, p99 `6002.000`, mean `4659.686`.
- Verdict: failed the real-win/Score>=2.0 keep gate because p50 regressed and the mean movement was not material.
- Source restored: no `sort.rs` change retained; restored source SHA256 `f54102e160c31e4c53f9cf18baeb08284f2a0dc26eebe6cb7d42e399c85537af`.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.45-qsort-three-way-partition.md`.
- Score after measurement: 0.0, rejected.

---

Completion update for `bd-2g7oyh.46`: qsort small-partition insertion block move shipped.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/stdlib/sort.rs::insertion_sort` on `qsort_128_i32`.
- Fresh post-`87695585` broad RCH profile on `vmi1167313`: `qsort_128_i32` FrankenLibC p50 `7182.823 ns/op`, p95 `7910.732`, p99 `8754.439`, mean `7070.474`; host p50 `4556.657`, p95 `5316.742`, p99 `5709.379`, mean `4499.363`.
- Focused pre-change RCH qsort baseline on `vmi1264463`: FrankenLibC p50 `7341.765 ns/op`, p95 `15465.841`, p99 `18053.184`, mean `8257.883`; host p50 `4724.885`, p95 `8558.040`, p99 `10761.861`, mean `5297.461`.
- Coordination: skipped BlackThrush's active `str.rs` lane and left peer-dirty allocator surfaces untouched.
- Lever: for element widths up to 64 bytes, replace small-partition insertion-sort adjacent swap chains with one stack-scratch save plus `copy_within` block shift; widths above 64 bytes keep the previous adjacent-swap path.

Pass 2 - Baseline and golden behavior: complete.
- Pre-change sort proof passed 25/25 tests on `vmi1153651`.
- Post-change sort proof passed 26/26 tests on `vmi1153651`, including local equal-key insertion order coverage.
- Golden fixture SHA256 stayed `stdlib_sort.json=ff7eb36ea363367e78b7287e53ecbd28ef43f0fed8337c864d446ae166be71b6`.
- Source SHA256 changed from `f54102e160c31e4c53f9cf18baeb08284f2a0dc26eebe6cb7d42e399c85537af` to `e37b948c321a70d7258f96039b5f5f604e993fd1f71e7423d68098796bc87b2a`.

Pass 3 - Isomorphism proof: complete.
- Stable insertion ordering is preserved because the insertion point is still the first slot after all prior elements with `compare(prev, item) <= 0`.
- The block move shifts exactly the intervening elements right by one slot in original order, then restores the saved item bytes at the insertion point, matching the adjacent-swap final sequence.
- Widths above 64 bytes use the original adjacent-swap algorithm exactly.
- Pivot selection, partitioning, recursion, comparator calls outside the small insertion fallback, `swap_chunks`, `bsearch`, `mergesort`, `heapsort`, and `radixsort` are unchanged.
- Qsort global stability remains unspecified; sorted multiset output and local insertion tie behavior are preserved. Floating-point and RNG behavior are unaffected.

Pass 4 - Re-benchmark and keep decision: shipped.
- Post RCH benchmark on `vmi1153651`: FrankenLibC p50 `4179.607 ns/op`, p95 `4839.000`, p99 `4897.097`, mean `4237.437`; host p50 `4032.619`, p95 `5336.482`, p99 `5794.817`, mean `4206.889`.
- Confirmation post RCH benchmark on `vmi1153651`: FrankenLibC p50 `4434.335 ns/op`, p95 `5233.542`, p99 `5861.000`, mean `4476.938`; host p50 `4085.234`, p95 `4743.583`, p99 `4863.201`, mean `4185.171`.
- Validation: RCH `cargo check -p frankenlibc-core --all-targets` passed; RCH `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` passed; local `rustfmt --edition 2024 --check crates/frankenlibc-core/src/stdlib/sort.rs` passed.
- Score after measurement: Impact 4 x Confidence 5 / Effort 2 = 10.0, retained.

---

Completion update for `bd-2g7oyh.47`: malloc hot log hit-rate snapshot cache rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/allocator.rs::record_lifecycle` on `malloc_free_64`.
- Fresh post-`da1bedd6` broad RCH profile on `vmi1149989`: `malloc_free_64` FrankenLibC p50 `160.666 ns/op`, p95 `198.966`, p99 `282.336`, mean `168.685`; host p50 `3.884`, p95 `10.625`, p99 `40.000`, mean `6.132`.
- Focused pre-change RCH baseline on `vmi1149989`: FrankenLibC p50 `173.142 ns/op`, p95 `232.157`, p99 `350.215`, mean `178.515`; host p50 `4.302`, p95 `8.188`, p99 `40.500`, mean `7.245`.
- Coordination: skipped BlackThrush's active `str.rs` lane and left peer-dirty `malloc/elimination.rs` untouched.
- Candidate lever: cache `cache_hit_rate_permille` in `MallocState` and refresh it only when `thread_cache_hits` or `thread_cache_misses` changed.

Pass 2 - Baseline and golden behavior: complete.
- Pre-change allocator proof passed 15/15 tests on `vmi1153651`.
- Post-change allocator proof passed 15/15 tests on `vmi1227854`, including `hot_cycle_lifecycle_record_sha256_is_stable`.
- Lifecycle golden SHA256 stayed `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof: complete.
- The candidate would preserve every recorded hit-rate value because the cached snapshot was refreshed immediately after each hit/miss counter increment and record creation only reads the latest counter state.
- Allocation/free ordering, thread-cache LIFO behavior, central-bin order, elimination order, pointer values, record schema, decision ids, and lifecycle trace ids were unchanged.
- Floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- Post RCH benchmark on `vmi1153651`: FrankenLibC p50 `334.072 ns/op`, p95 `399.520`, p99 `521.000`, mean `345.745`; host p50 `8.219`, p95 `15.125`, p99 `47.500`, mean `11.050`.
- Verdict: failed the real-win/Score>=2.0 keep gate because p50 and mean materially regressed.
- Source restored: no `allocator.rs` change retained; restored source SHA256 `6bb73dd97daef30f2c596931f0dec11e1014837733fee23a44586df56445716d`.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.47-malloc-hit-rate-cache.md`.
- Score after measurement: 0.0, rejected.

---

Completion update for `bd-2g7oyh.48`: malloc thread-cache fixed magazine array rejected.

Pass 1 - Profile-backed target selection: complete.
- Target: `crates/frankenlibc-core/src/malloc/thread_cache.rs::ThreadCache` on `malloc_free_64`.
- Focused RCH baseline on `vmi1149989` from the unchanged source after bd47 rejected: FrankenLibC p50 `173.142 ns/op`, p95 `232.157`, p99 `350.215`, mean `178.515`; host p50 `4.302`, p95 `8.188`, p99 `40.500`, mean `7.245`.
- Candidate lever: replace the always-32-entry `Vec<Magazine>` table with `[Magazine; NUM_SIZE_CLASSES]` initialized by `std::array::from_fn`.

Pass 2 - Baseline and golden behavior: complete.
- Pre-change thread-cache proof passed 9/9 tests on `vmi1153651`.
- Pre-change allocator proof passed 15/15 tests on `vmi1227854`.
- Post-change thread-cache proof passed 9/9 tests on `vmi1227854`.
- Post-change allocator proof passed 15/15 tests on `vmi1156319`, including `hot_cycle_lifecycle_record_sha256_is_stable`.
- Lifecycle golden SHA256 stayed `01df8806e2bfd0fda041e153ec61ec4737ad2d3cb1ce22050a2e35bab1688455`.

Pass 3 - Isomorphism proof: complete.
- Magazine count stayed exactly `NUM_SIZE_CLASSES`.
- Each per-class `Magazine` still owned the same per-class `Vec<usize>` stack, so LIFO allocation order, capacity checks, drain order, invalid-index behavior, and `total_cached` accounting were unchanged.
- Allocator lifecycle record schema, decision ids, trace ids, pointer values, allocation/free ordering, central-bin behavior, and elimination behavior were unchanged.
- Floating-point and RNG behavior were unaffected.

Pass 4 - Re-benchmark and keep/reject decision: rejected.
- First post RCH benchmark on `vmi1227854`: FrankenLibC p50 `141.945 ns/op`, p95 `181.226`, p99 `260.000`, mean `151.051`; host p50 `4.262`, p95 `6.250`, p99 `35.000`, mean `5.773`.
- Same-worker confirmation on `vmi1149989`: FrankenLibC p50 `182.005 ns/op`, p95 `201.126`, p99 `400.000`, mean `177.248`; host p50 `3.468`, p95 `6.562`, p99 `40.000`, mean `5.338`.
- Verdict: failed the real-win/Score>=2.0 keep gate because same-worker p50 regressed and mean movement was not material.
- Source restored: no `thread_cache.rs` change retained; restored source SHA256 `bafae8d9dea4651dd5c69ca66c4592ad10fa59ed181fc2662c630b6ce176fbdd`.
- Proof artifact: `tests/artifacts/perf/bd-2g7oyh.48-thread-cache-array.md`.
- Score after confirmation: 0.0, rejected.
