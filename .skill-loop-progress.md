# Repeated Skill Loop Progress

Objective: apply extreme-software-optimization repeatedly to `bd-tti4cb` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/HOTSPOT_TABLE.md`.

Pass 1 - Profile triage and target scoring: complete.
- Target: `ValidationPipeline::validate` fast-path gating in `crates/frankenlibc-membrane/src/ptr_validator.rs`.
- Evidence: profiler handoff reports `validate_null` about 2131 ns p50 and `validate_known` about 2095 ns p50 while isolated stages are <=13 ns.
- Candidate lever: run dependency-forced null, TLS-cache-hit, and empty-page-oracle exits before the runtime-feedback gate when validation logging is disabled.
- Score: Impact 5 x Confidence 4 / Effort 2 = 10.0.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench membrane_bench validate_`.
- Baseline p50 strict: `validate_null=3095.022 ns`, `validate_known=3060.201 ns`, `validate_foreign=1609.716 ns`, `validate_foreign_nonempty_oracle=1584.457 ns`.
- Golden command: `RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-membrane --lib ptr_validator::tests::default_cache_hit_fast_path_matches_logged_pipeline_hit -- --exact --nocapture`.
- Golden transcript sha256: `5dcc160709f115dc62a68e2ed7ea3e099451d96174aa3ea657b9c17706b9b80f`.
- Golden normalized exact-test-line sha256: `cc4b1a202746214e8b85c175c337b9203e88fd8326c7ef51de5d35755f5faa14`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Safety-facing outputs are preserved by tests comparing no-log fast exits to logged full-path outcomes for null, TLS-cache, and empty-oracle foreign pointers.
- Full validation ordering/tie-breaking is unchanged: `validate_with_security_context` still owns `check_ordering`, `decide`, and all full-path `observe_validation_result` calls.
- Floating-point and RNG behavior are unaffected: the edited path performs only integer/null/cache/oracle checks and does not touch runtime-math stochastic controllers.
- Logging behavior is preserved: logging enabled still routes through the full trace path; logging disabled emits no validation log rows.

Pass 4 - One-lever implementation: complete.
- Lever: when validation logging is disabled, `ValidationPipeline::validate` uses dependency-forced null, TLS-cache-hit, and empty-page-oracle exits before trace construction/runtime feedback.
- Full validation ordering, runtime decisions, and feedback remain on the existing runtime-math pipeline for non-fast-path validations.
- Note: a feedback-preserving variant was measured and rejected because it did not clear the score gate on same-worker rch criterion.

Pass 5 - Re-benchmark and validation: complete.
- After command: `RCH_VISIBILITY=summary rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench membrane_bench -- validate_ --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- After p50 strict: `validate_null=11.220 ns`, `validate_known=19.528 ns`, `validate_foreign=18.602 ns`, `validate_foreign_nonempty_oracle=1362.349 ns`.
- Before/after p50 vs current-HEAD rch baseline: `validate_null 3008.069 -> 11.220 ns`, `validate_known 3078.129 -> 19.528 ns`, `validate_foreign 1623.562 -> 18.602 ns`.
- Confirmation command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench membrane_bench validate_`.
- Confirmation p50 strict: `validate_null=10.002 ns`, `validate_known=17.055 ns`, `validate_foreign=16.911 ns`, `validate_foreign_nonempty_oracle=1091.712 ns`.
- Behavior proof: focused rch tests compare fast no-log outcomes to logged full-path outcomes for null, TLS-cache-hit, and empty-oracle foreign pointers; final run passed 3/3.
- Golden sha256 stable: `tests/conformance/tls_cache_epoch_invalidation.golden.json = 53ede3d727fbf061d608bf50c10b2faa9873716eeb517e9ca1276edbc3e881c1`.
- Golden normalized exact-test-line sha256 matched baseline: `cc4b1a202746214e8b85c175c337b9203e88fd8326c7ef51de5d35755f5faa14`.
- Validation: `cargo test -p frankenlibc-membrane --lib ptr_validator::tests::default_ -- --nocapture`, `cargo check -p frankenlibc-membrane --all-targets`, and `cargo clippy -p frankenlibc-membrane --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-membrane` passed locally because rch treats fmt as non-compilation.
- UBS: scanned `crates/frankenlibc-membrane/src/ptr_validator.rs`; nonzero due pre-existing test-only panic/unwrap/assert inventory, with no new critical finding in the edited fast-path production code.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization repeatedly to `bd-6ypsli` using the profiler handoff in `tests/artifacts/perf/20260601-perf-26a69011/string_bench.summary.md`.

Pass 1 - Profile triage and target scoring: complete.
- Target: `frankenlibc_core::string::mem::memcmp` in `crates/frankenlibc-core/src/string/mem.rs`.
- Evidence: profiler handoff reports equal 4096-byte `memcmp` at `1255 ns` while same-suite `memcpy` is `31.6 ns`; fresh rch baseline measured `1593.221 ns` p50 for 4096 bytes.
- Candidate lever: compare fixed 8-byte chunks for equality, falling back to byte-order comparison only inside a differing chunk.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench memcmp -- --sample-size 40`.
- Baseline p50 raw: `memcmp_16=7.759 ns`, `memcmp_64=26.353 ns`, `memcmp_256=108.320 ns`, `memcmp_1024=402.212 ns`, `memcmp_4096=1593.221 ns`.
- Golden command: `RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-core memcmp -- --nocapture`.
- Golden normalized existing-test-line sha256: `eef1f26220f1736c43e59f193a2b8b1fb9a8ecf5dbb2a5abf2fb31071ac18312`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering is preserved because chunk words are used only for equality rejection; first differing byte and unsigned tie-break are still computed by byte scan in original order.
- Partial-length behavior is preserved by slicing both inputs to `min(n, a.len(), b.len())` before chunking.
- Floating-point and RNG behavior are unaffected: the edited function performs only integer byte comparisons and does not touch global state.
- Added a targeted regression for first-difference ordering inside an 8-byte bulk chunk and unsigned tail ordering.

Pass 4 - One-lever implementation: complete.
- Lever: safe Rust 8-byte chunk equality with byte fallback on chunk mismatch and tail bytes.
- No unsafe reads, no endian ordering shortcut, no heap allocation, and no benchmark harness changes.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench string_bench memcmp -- --sample-size 40`.
- Post p50 raw: `memcmp_16=2.917 ns`, `memcmp_64=5.842 ns`, `memcmp_256=15.859 ns`, `memcmp_1024=62.250 ns`, `memcmp_4096=208.501 ns`.
- Before/after p50 raw: `memcmp_16 7.759 -> 2.917 ns`, `memcmp_64 26.353 -> 5.842 ns`, `memcmp_256 108.320 -> 15.859 ns`, `memcmp_1024 402.212 -> 62.250 ns`, `memcmp_4096 1593.221 -> 208.501 ns`.
- Golden normalized existing-test-line sha256 matched baseline: `eef1f26220f1736c43e59f193a2b8b1fb9a8ecf5dbb2a5abf2fb31071ac18312`; full post-test-line sha256 including the new regression is `2de28db68bbcf8edcaab17df8dad9f9e63915076fa6528c41ce1f78b1b388fd4`.
- Validation: `cargo test -p frankenlibc-core memcmp -- --nocapture`, `cargo check -p frankenlibc-core --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check -p frankenlibc-core`, `git diff --check`, and `ubs crates/frankenlibc-core/src/string/mem.rs` passed locally.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization repeatedly to `bd-wvxyzs` using the profiler handoff for `runtime_math::design::OptimalDesignController::choose_plan`.

Pass 1 - Profile triage and target scoring: complete.
- Target: `OptimalDesignController::choose_plan` in `crates/frankenlibc-membrane/src/runtime_math/design.rs`.
- Evidence: bead profiler handoff reports `design_choose_plan` about `1008.7 ns` p50 with root cause at `design.rs:137`: copy the Fisher matrix, apply a rank-one update, and recompute a full Cholesky/logdet for every candidate probe.
- Fresh rch baseline measured `design_choose_plan=1747.061 ns` p50 on worker `ts2`.
- Candidate lever: compute the baseline Cholesky once and score rank-one candidate gains with the matrix determinant lemma, falling back to the old full-logdet path if the matrix is not SPD.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench runtime_math_kernels_bench design_choose_plan -- --sample-size 50`.
- Baseline p50 strict: `design_choose_plan=1747.061 ns` (`p95=1845.895 ns`, `p99=1903.000 ns`).
- Golden command: `RCH_VISIBILITY=summary rch exec -- cargo test -p frankenlibc-membrane runtime_math::design -- --nocapture`.
- Golden sorted existing-test-line sha256: `32b0990b201b4404c2bab4c093c51badd5f4009a646a41c9b0dd9c3ee96080b3`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering and tie-breaking are preserved because candidate order, `sort_by` tie handling, budget pruning, fallback probe insertion, and record/update paths are unchanged.
- Floating-point semantics are equivalent for SPD Fisher matrices by `det(A + wvv^T) = det(A) * (1 + w * v^T A^-1 v)`, using the same Cholesky factorization already used by `logdet_spd`.
- Non-SPD behavior is preserved by falling back to the previous full-copy `rank_one_update` plus full `logdet_spd` candidate scoring path.
- RNG and external state are unaffected: the edit only changes deterministic matrix scoring inside `choose_plan`.

Pass 4 - One-lever implementation: complete.
- Lever: factor the current Fisher matrix once, reuse the Cholesky factor for per-probe rank-one logdet gains, and keep the old full-logdet path as the non-SPD fallback.
- Added a regression test that compares the determinant-lemma gain against the full matrix-copy/update/logdet computation for every probe.
- No benchmark harness changes and no changes to probe costs, budgets, risk thresholds, mask handling, or plan construction.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `RCH_VISIBILITY=summary rch exec -- cargo bench -p frankenlibc-bench --bench runtime_math_kernels_bench design_choose_plan -- --sample-size 50`.
- Post p50 strict: `design_choose_plan=432.283 ns` (`p95=490.500 ns`, `p99=607.469 ns`).
- Before/after p50 strict: `design_choose_plan 1747.061 -> 432.283 ns`.
- Golden sorted existing-test-line sha256 matched baseline: `32b0990b201b4404c2bab4c093c51badd5f4009a646a41c9b0dd9c3ee96080b3`; full post-test-line sha256 including the new regression is `e3bed02139b6cc5502c48f3a9384feb5477641cba629ac4d1e4e11965b3f14db`.
- Validation: `cargo test -p frankenlibc-membrane runtime_math::design -- --nocapture`, `cargo check -p frankenlibc-membrane --all-targets`, and `cargo clippy -p frankenlibc-membrane --all-targets -- -D warnings` all passed via rch after the final edit; `cargo fmt --check -p frankenlibc-membrane` and `git diff --check` passed locally because rch treats fmt as non-compilation.
- UBS: scanned `crates/frankenlibc-membrane/src/runtime_math/design.rs`; no critical findings. The new test was adjusted to avoid adding an `expect` warning.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

---

Objective: apply extreme-software-optimization to `bd-yftnsz` using the profiler handoff for `stdio::printf::parse_format_string`.

Pass 1 - Profile triage and target scoring: complete.
- Target: `parse_format_string` in `crates/frankenlibc-core/src/stdio/printf.rs`.
- Evidence: bead profiler handoff reports `stdio_printf_parse/literal=26.8 ns` for a percent-free literal format, with root cause identified as per-call owned `Vec<FormatSegment>` allocation.
- Fresh rch baseline measured `literal=40.542 ns`, `mixed=169.74 ns`, `positional=153.32 ns` on worker `ts2`.
- Candidate lever: preserve the existing parser but replace the returned `Vec` with a small segment container that avoids heap allocation for one-segment formats and falls back to an 8-capacity `Vec` for multi-segment formats.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.

Pass 2 - Baseline and golden behavior: complete.
- Baseline command: `rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench stdio_bench -- stdio_printf_parse --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- Baseline p50 raw: `literal=40.542 ns`, `mixed=169.74 ns`, `positional=153.32 ns`.
- Golden sha256: `tests/conformance/fixtures/printf_conformance.json=b8657a70042071e59636fe167d7ffdfb6ae25dab77a173056cec1465ae27c6ad`, `tests/conformance/printf_star_width_precision_completion_contract.v1.json=8d95e8578dc4234246b5537dcd99fbe1c865caa3059c695aa595491d2272c4ed`, `tests/conformance/stdio_printf_overflow_integration_completion_contract.v1.json=f9a3136fe8fadc05be40e89bd6025d60f1d4e2626dfb98d7f102b1fd112ac3c8`.

Pass 3 - Isomorphism proof and edit plan: complete.
- Ordering and tie-breaking are preserved because the byte parser loop, `parse_format_spec`, segment push order, positional width/precision handling, and render/count consumers are unchanged.
- Floating-point and RNG behavior are unaffected: the edit only changes segment container storage and does not change formatting math, argument extraction order, global state, or random sources.
- Public caller behavior is preserved by `Deref<Target=[FormatSegment]>`, borrowed iteration, and owned iteration compatibility for existing `&segments`, indexing, `.len()`, and `for seg in parse_format_string(...)` callers.

Pass 4 - One-lever implementation: complete.
- Lever: `FormatSegments` stores zero or one segment inline and falls back to a `Vec` with capacity 8 on the second segment; `FormatSpec` and `FormatSegment` are `Copy` to support safe owned iteration without cloning heap data.
- No benchmark harness changes, no global parse cache, no mutex, no parser grammar changes, and no ABI argument extraction changes.

Pass 5 - Re-benchmark and validation: complete.
- Post command: `rch exec -- env FRANKENLIBC_BENCH_PIN=1 cargo bench -p frankenlibc-bench --bench stdio_bench -- stdio_printf_parse --sample-size 50 --measurement-time 3 --warm-up-time 1`.
- Post p50 raw: `literal=18.997 ns`, `mixed=94.683 ns`, `positional=99.395 ns`.
- Before/after p50 raw: `literal 40.542 -> 18.997 ns`, `mixed 169.74 -> 94.683 ns`, `positional 153.32 -> 99.395 ns`.
- Golden sha256 unchanged for the three printf conformance/contract fixtures listed above.
- Validation: `cargo test -p frankenlibc-core printf -- --nocapture`, `cargo test -p frankenlibc-core fmtcheck -- --nocapture`, `cargo check -p frankenlibc_conformance --all-targets`, and `cargo clippy -p frankenlibc-core --all-targets -- -D warnings` all passed via rch; `cargo fmt --check` passed locally because rch treats fmt as non-compilation.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5.
